# Iterations Summary

> Single-file context for future conversations. Replaces reading 29 individual iteration docs.
> Last updated: iter 097 closed on 2026-04-30. Phase 4.6 Tier 1 ports `/shipments/[id]` under the `(nexus)` AppShell with new `ShipmentDetailHeader` + `ShipmentMetaPanel` + `ShipmentLineItemsPanel` on Phase 4.0 primitives. No new functional surface — iter 046 frontend (docs / readiness / mark-ready) and iter 074 frontend (booking / mark-shipped) follow as separate iters. Remaining pre-revamp routes: `/login` / `/register` / `/setup` (auth-page revamp couples with the iter 096 invite-token migration). Earlier today: iter 094 (Phase 4.5 Tier 4 cert UI), iter 095 (ADMIN user-management CRUD), iter 096 (invite-token UUID for registration link). Iter 098 (credential-reset + reissue-invite) in flight on a parallel branch.

---

## Product vision

Turbo Tonic is a vendor portal for purchase order confirmation, invoicing, production tracking, and shipment booking and tracking logistics. The CRUD workflow is table stakes. The differentiator is an agent layer that reads, validates, and flags documents (shipment docs, invoices, customs declarations). Two exit paths after customs validation ships: (a) horizontal B2B SaaS for procurement/logistics/invoicing companies, or (b) vertical industry specialization.

## Tech stack

Python 3.13, FastAPI, Postgres 16 (asyncpg, connection pool), WebAuthn/passkeys + cookie sessions. Frontend: SvelteKit 2 + Svelte 5, adapter-static. Package manager: uv (backend), npm (frontend), nvm for Node. Local dev: Postgres via Homebrew (docker-compose.yml available for Docker environments).

## Current domain model (as of iteration 046)

### Aggregates
- **PurchaseOrder** — Draft > Pending > MODIFIED (ping-pong with vendor) > Accepted | Rejected (only via all-REMOVED convergence) > Revised > Pending. Contains LineItems each with LineItemStatus (PENDING/MODIFIED_BY_VENDOR/MODIFIED_BY_SM/ACCEPTED/REMOVED), line_edit_history. PO-scoped round_count (0-2), last_actor_role. Types: PROCUREMENT, OPEX.
- **Invoice** — Draft > Submitted > Approved > Paid | Disputed > Resolved. Linked to PO. Over-invoicing guard checks cumulative quantities against PO line items.
- **Vendor** — Active | Inactive. Types: PROCUREMENT, OPEX, FREIGHT, MISCELLANEOUS. Country validated against reference data.
- **Product** — Vendor-scoped catalog (vendor_id + part_number unique). Qualifications managed via QualificationType join table (replaces requires_certification boolean).
- **QualificationType** — Named qualification requirement (e.g. QUALITY_CERTIFICATE). Linked to products via join table. Target market scoped.
- **Certificate** — Links product to qualification type. Status: Pending > Valid. EXPIRED computed from expiry_date. Tracks cert_number, issuer, testing_lab, test/issue/expiry dates, target_market. Document attachment via file storage.
- **PackagingSpec** — Per-product per-marketplace packaging requirement. Status: Pending > Collected (via file upload). Document attachment via file storage. Unique on (product_id, marketplace, spec_name).
- **Shipment** — DRAFT > DOCUMENTS_PENDING > READY_TO_SHIP > BOOKED > SHIPPED. Linked to PO; multiple shipments per PO. ShipmentLineItems carry net_weight, gross_weight, package_count, dimensions, country_of_origin (all nullable, settable in DRAFT/DOCUMENTS_PENDING). Booking metadata (carrier, booking_reference, pickup_date) populated on `book_shipment` transition; `shipped_at` set on `mark_shipped`. Cumulative shipped quantity per part_number cannot exceed PO accepted quantity. Marketplace inherited from PO.
- **ShipmentDocumentRequirement** — PENDING > COLLECTED. Per-shipment document checklist. Default PACKING_LIST and COMMERCIAL_INVOICE rows (is_auto_generated=True) seeded on submit-for-documents; SM/FREIGHT_MANAGER may add custom user-defined document_type rows. Auto-generated rows always pass the documents readiness check (PDFs render on demand).

### Supporting concepts
- **Milestone** — RAW_MATERIALS > PRODUCTION_STARTED > QC_PASSED > READY_FOR_SHIPMENT > SHIPPED. Strict sequence enforcement. Attached to PO. (READY_FOR_SHIPMENT renamed from READY_TO_SHIP in iter 074 to disambiguate from the per-shipment status.)
- **ActivityLog** — Immutable event stream. Categories: LIVE, ACTION_REQUIRED, DELAYED. Target roles: SM, VENDOR, QUALITY_LAB, FREIGHT_MANAGER. Entity types: PO, INVOICE, CERTIFICATE, PACKAGING, SHIPMENT. Powers dashboard feed and notification bell.
- **CertWarning** (iter 039) — Advisory result of the PO-submit quality gate. One entry per (line item, missing/expired qualification, marketplace).
- **ReadinessResult** (iter 046) — Composite of documents_ready + certificates_ready + packaging_ready with structured missing-item lists. Drives the mark-ready gate.
- **Reference data** — 30 currencies, 11 incoterms, 17 payment terms, 31 countries, 50+ ports, USD exchange rates.

### Database tables
purchase_orders, line_items, rejection_history, vendors, products, invoices, invoice_line_items, milestone_updates, activity_log, files (with `uploaded_by` since iter 084), qualification_types, product_qualifications, certificates, packaging_specs, line_edit_history, shipments, shipment_line_items, shipment_document_requirements.

## Frontend routes

| Route | Purpose |
|-------|---------|
| `/dashboard` | (Phase 4.1) ADMIN/SM 4-KPI grid (Pending POs, Awaiting acceptance, In production, Outstanding A/P) + recent activity + awaiting-acceptance panel under `(nexus)` AppShell. Other roles see a placeholder pending their dashboard iter. |
| `/po` | PO list with filters, search, sort, pagination, bulk actions |
| `/po/new` | Create PO form |
| `/po/[id]` | PO detail: status actions, line items, invoices, milestones, activity timeline, PDF |
| `/po/[id]/edit` | Edit rejected PO for revision |
| `/invoices` | Invoice list with filters, bulk PDF download |
| `/invoice/[id]` | Invoice detail: status actions, line items, activity timeline, PDF |
| `/vendors` | (Phase 4.4) Vendor list under `(nexus)` AppShell with status/type filters, status pills, Deactivate/Reactivate action buttons, empty/loading/error states, mobile cards |
| `/vendors/new` | (Phase 4.4) Create vendor under `(nexus)` AppShell |
| `/products` | (Phase 4.5) Product catalog list under `(nexus)` AppShell with vendor filter, qualification count pill, empty/loading/error states, mobile cards |
| `/products/new` | (Phase 4.5) Create product under `(nexus)` AppShell |
| `/products/[id]/edit` | (Phase 4.5) Edit product attributes + qualifications + packaging specs + certificates panels under `(nexus)` AppShell |
| `/shipments/[id]` | (Phase 4.6) Shipment detail under `(nexus)` AppShell: editable per-line weights/dims/CoO via `ShipmentLineItemsPanel`, header `StatusPill` covering all 5 statuses, Packing List + Commercial Invoice PDF downloads |
| `/login` | WebAuthn passkey login, deep link redirect support |
| `/register` | Invite-only passkey registration (username from query param) |
| `/setup` | First-user bootstrap, pending-user message, already-configured detection |

## API surface

- **PO**: CRUD, submit, accept, resubmit, bulk transition, PDF export. Submit/resubmit return `POSubmitResponse` (po + cert_warnings) per iter 039.
- **PO line negotiation**: per-line modify, accept, remove, force-accept, force-remove; submit-response for round hand-off
- **Invoice**: CRUD, submit, approve, pay, dispute, resolve, remaining quantities, PDF export, bulk PDF
- **Vendor**: CRUD, deactivate, reactivate
- **User (iter 095)**: ADMIN-only management on `/api/v1/users`: `POST /invite` (since iter 030/031), `GET /` with optional `status` + `role` filters, `GET /{id}`, `PATCH /{id}` (display_name + email), `POST /{id}/deactivate` (last-active-admin guard fires before self-deactivate guard), `POST /{id}/reactivate`. Role/vendor_id/username mutation deferred. UserDict now exposes `email`.
- **Auth — invite token (iter 096)**: `POST /api/v1/auth/bootstrap` and `POST /api/v1/users/invite` return `invite_token` alongside the user dict. Registration handshake is keyed on the token: `POST /api/v1/auth/register/options` and `POST /api/v1/auth/register/verify` take `{token}` instead of `{username}`. Token is a UUID v4, generated by `User.invite()`, cleared by `User.activate()` (consume-once). `_user_to_dict` does not expose it. Login flow unchanged.
- **Product**: CRUD (POST, GET list, GET by id, PATCH), packaging readiness per marketplace
- **Milestone**: list by PO, post milestone. QC_PASSED triggers CERT_REQUESTED activity for products lacking valid certs (iter 039).
- **Shipment**: create, list (by PO or all), get, remaining-quantities, PATCH line item weights/dims, submit-for-documents, mark-ready (gated by readiness check)
- **Shipment documents**: list requirements, add custom requirement, upload file against requirement, GET readiness (documents + certificates + packaging composite)
- **Shipment PDFs**: GET packing-list, GET commercial-invoice (CI number deterministic, not persisted)
- **Dashboard**: legacy `/dashboard/` returns the pre-revamp aggregate (kept additively during the revamp); `/dashboard/summary` (iter 071) returns role-scoped KPIs + awaiting-acceptance list + activity feed for the `(nexus)` page. Pre-revamp endpoint will retire end of Phase 4.
- **Activity**: list (with optional target_role filter), unread count (with optional target_role filter), mark read
- **Reference data**: all lookups in one GET
- **Document**: upload (multipart, PDF-only, 10MB limit), download (Content-Disposition), delete, list by entity (any authed; pre-iter-084 generic surface still open per backlog)
- **PO documents (iter 084)**: PO-scoped endpoints under `/api/v1/po/{po_id}/documents` (POST upload, GET list with denormalized `uploaded_by_username`, GET download with Content-Disposition, DELETE). Role + ownership + PO-type guards layered over iter 035 storage. File-type vocabulary partitioned by PO type via `validate_attachment_type`.
- **QualificationType**: CRUD, product assignment/removal
- **Certificate**: CRUD, document upload, computed EXPIRED status. List item DTO carries `document_id` since iter 094.
- **PackagingSpec**: CRUD, file upload, packaging readiness per product/marketplace

---

## Iteration log

| Iter | Date | What was done |
|------|------|---------------|
| 01 | 2026-03-12 | Project scaffold: FastAPI + SvelteKit, SQLite, PO create/list |
| 02 | 2026-03-16 | PO detail page, status transitions (submit/accept/reject), revision flow |
| 03 | 2026-03-17 | Vendor entity: CRUD, active/inactive lifecycle |
| 04 | 2026-03-19 | Vendor reactivation, reference data dropdowns (currencies, incoterms, payment terms) |
| 05 | 2026-03-19 | PO PDF export (backend + download) |
| 06 | 2026-03-24 | PO list: filtering by status/vendor/currency, text search, pagination |
| 07 | 2026-03-24 | Dashboard: PO summary cards, vendor counts, recent POs |
| 08 | 2026-03-26 | Bulk PO status transitions (submit, accept, reject multiple) |
| 09 | 2026-03-26 | UI polish: bulk action feedback, status pill component |
| 10 | 2026-03-26 | Cross-page selection for bulk actions (up to 200 IDs) |
| 11 | 2026-03-30 | Vendor types (Procurement, OpEx, Freight, Miscellaneous), PO type derived from vendor |
| 12 | 2026-03-30 | Invoice module: domain model, create from PO, over-invoicing guard, submit/approve/pay/dispute |
| 13 | 2026-03-30 | Label resolution: reference data codes to human labels in UI and PDFs |
| 14 | 2026-03-30 | Invoice quantity control: remaining quantities endpoint, UI for partial invoicing |
| 15 | 2026-03-30 | Invoice list page with filters (status, PO#, vendor, invoice#, date range), pagination |
| 16 | 2026-03-30 | Invoice PDF export (backend) |
| 17 | 2026-03-30 | Production milestones: domain model, strict sequence enforcement, API |
| 18 | 2026-03-30 | Milestone timeline UI on PO detail, post-milestone form |
| 19 | 2026-03-30 | Dashboard production pipeline: milestone counts, overdue detection |
| 20 | 2026-04-03 | Cosmetic: PO PDF cleanup, vendor country dropdown from reference data, HS code format validation |
| 21 | 2026-04-03 | OPEX invoicing: OpEx POs get free-form invoice lines (no PO line item linkage) |
| 22 | 2026-04-03 | Invoice PDF export (frontend): download button, bulk PDF download |
| 23 | 2026-04-06 | Activity log: event recording for PO/invoice/milestone actions, dashboard feed |
| 24 | 2026-04-06 | Notification bell: unread count, mark-read, activity timeline on detail pages |
| 25 | 2026-04-06 | Vendor country validation: domain-level reject of invalid country codes |
| 26 | 2026-04-06 | Playwright permanent test suite: activity feed, notification bell, timeline specs |
| 27 | 2026-04-06 | Field-level mutability rules: which PO fields are editable in which status |
| 28 | 2026-04-06 | Partial PO acceptance (line-item level accept/reject) — scoped but not started |
| 29 | 2026-04-09 | Product master: vendor-scoped product catalog with requires_certification flag |
| 29a | 2026-04-15 | Postgres migration: replaced aiosqlite with asyncpg connection pool, all repos use $N placeholders, test isolation via rolled-back transactions, seed script for demo data |
| 030 | 2026-04-15 | User entity (6 roles, 3 statuses), WebAuthn passkey registration/login, cookie sessions, session middleware, bootstrap/invite flows, critical-path integration test |
| 031 | 2026-04-15 | API role guards: require_role/require_auth on all 30+ endpoints, ADMIN bypass, bulk transition fine-grained action check, authenticated_client test fixture, dev-login endpoint |
| 032 | 2026-04-16 | Vendor-scoped data access: VENDOR users see only their vendor's POs, invoices, milestones, activity, and dashboard data. check_vendor_access helper, 404 on ownership mismatch, bulk ops skip non-owned entities |
| 033 | 2026-04-16 | Frontend auth flow: login page (WebAuthn passkey), invite-only register page, bootstrap/setup page, layout session check with redirect, logout, 401 handling, deep link preservation, credentials: include on all API calls, 9 new Playwright auth tests, existing tests updated with auth mock |
| 034 | 2026-04-16 | Frontend role-conditional rendering: permissions.ts helper, role-based nav links, PO/invoice/vendor/product page button guards, page-level role redirects via +page.ts, dashboard role-filtered widgets, backend target_role filter on activity endpoints, 16 new Playwright role tests |
| 035 | 2026-04-16 | Document storage infrastructure: files table, FileMetadata domain model, DocumentRepository, FileStorageService (local disk), upload/download/delete/list API endpoints, PDF-only 10MB limit, path traversal protection, require_auth on all endpoints, 19 new tests |
| 036 | 2026-04-16 | Model extensions: marketplace on PO (with reference data validation), product_id on LineItem, address + account_details on Vendor, manufacturing_address on Product. Schema migration via ALTER TABLE. Frontend forms updated. Marketplace filter on PO list. |
| 036a | 2026-04-16 | QualificationType entity replacing requires_certification boolean, product-qualification join table, CRUD + assignment API, 35 new tests |
| 037 | 2026-04-16 | Per-line-item accept/reject on POs: LineItemStatus enum, accept_lines() method, per-line toggle UI, vendor-only Submit Response, 16 new tests |
| 041 | 2026-04-16 | PackagingSpec entity for per-product per-marketplace packaging requirements, CRUD API, product edit UI section, 32 new tests |
| 038 | 2026-04-16 | Certificate entity linking products to qualification types with cert details, computed EXPIRED status, document upload, CERT_UPLOADED activity event, 27 new tests |
| 042 | 2026-04-16 | PackagingSpec file collection: COLLECTED status, document upload, packaging readiness endpoint, PACKAGING_COLLECTED activity event, 15 new tests |
| 056 | 2026-04-19 | Line-level negotiation domain + API: replaces iter 037 accept_lines with modify/accept/remove/force-accept/force-remove/submit-response per-line endpoints, 2-round cap, convergence to ACCEPTED/REJECTED, line_edit_history table, new statuses MODIFIED_BY_VENDOR/MODIFIED_BY_SM/REMOVED, new PO status MODIFIED. Drops `reject()` and `/reject` entirely. 70+ new permanent tests. |
| 057 | 2026-04-19 | Negotiation UI: LineNegotiationRow + ModifyLineModal + LineDiff + EditHistoryTimeline + SubmitResponseBar components; PO detail page replaces RejectDialog/accept-lines-table with per-line modify/accept/remove controls; Force Accept and Force Remove with confirmation dialog at round 2 for SM; 8 new Playwright specs. |
| 058 | 2026-04-19 | PDF scoping + negotiation activity events: PO PDF filters to ACCEPTED lines only with MODIFIED stamp when round_count >= 1; 7 new event types (PO_LINE_MODIFIED, PO_LINE_ACCEPTED, PO_LINE_REMOVED, PO_FORCE_ACCEPTED, PO_FORCE_REMOVED, PO_MODIFIED, PO_CONVERGED); router wires events on every per-line endpoint; Partial + Modified pills on PO list. 15 new permanent tests. |
| 059 | 2026-04-19 | Advance payment gate + post-acceptance line modification: payment_terms metadata with has_advance flag (4 terms flagged), advance_paid_at column on PO, mark_advance_paid + add_line_post_acceptance + remove_line_post_acceptance methods + endpoints (SM-only), downstream-artifact check via services layer, 3 new activity events (PO_ADVANCE_PAID, PO_LINE_ADDED_POST_ACCEPT, PO_LINE_REMOVED_POST_ACCEPT), minimal frontend UI. 37 new permanent tests. |
| 060 | 2026-04-19 | Email notifications: aiosmtplib-backed EmailService with Jinja2 templates (po_accepted, po_modified, po_line_modified, po_advance_paid), NotificationDispatcher decoupled from activity repo, recipient resolution per role and vendor scope, users.email column + seed backfill, EMAIL_SEND_FAILED activity event on delivery failure, FakeEmailService fixture default in tests. 15 new permanent tests. |
| 043 | 2026-04-24 | Shipment aggregate: shipments + shipment_line_items tables, DRAFT > DOCUMENTS_PENDING > READY_TO_SHIP, create from accepted PO with cumulative shipped-quantity guard, create/list/get/remaining-quantities endpoints, role guards (SM + FREIGHT_MANAGER for mutations). |
| 039 | 2026-04-24 | Quality gate at PO submit + CERT_REQUESTED on QC_PASSED: services/quality_gate.py service iterates accepted line items with product_id, checks valid certs against PO marketplace, returns CertWarning list. PO submit/resubmit return new POSubmitResponse wrapper (po + cert_warnings). QC_PASSED milestone fans out CERT_REQUESTED activity to QUALITY_LAB. TargetRole.QUALITY_LAB and FREIGHT_MANAGER added to enum. ~20 new tests. |
| 044 | 2026-04-24 | Shipment line item weights/dims + packing list PDF: net_weight/gross_weight/package_count/dimensions/country_of_origin (all nullable) + Shipment.update_line_items() guarded by status. PATCH /shipments/{id} (SM + FREIGHT_MANAGER), GET /shipments/{id}/packing-list returning ReportLab PDF (SM + VENDOR + FREIGHT_MANAGER). Frontend shipment detail page with inline weight/dim editing and download button. ~10 new tests. |
| 045 | 2026-04-24 | Export commercial invoice PDF on Shipment: GET /shipments/{id}/commercial-invoice. Deterministic CI number CI-{shipment_number}, not persisted. HS code + unit price from PO line items (matched by part_number); quantity + weights + country of origin from shipment line items. Same ReportLab pattern as packing list. Frontend Download Commercial Invoice button alongside Packing List. 7 new tests. |
| 046 | 2026-04-24 | Shipment document requirements + readiness gate (backend): shipment_document_requirements table, ShipmentDocumentRequirement entity (PENDING > COLLECTED), ReadinessResult composite (documents + certificates + packaging). Default PACKING_LIST and COMMERCIAL_INVOICE auto-generated rows seeded on submit-for-documents; both always pass docs check. New endpoints: POST /requirements (custom), POST /documents/{rid}/upload, GET /requirements, GET /readiness. Existing mark-ready endpoint enhanced with readiness gate (409 + ReadinessResult on failure). DOCUMENT_UPLOADED ActivityEvent + EntityType.SHIPMENT added. 12 new tests. Frontend (Documents section, readiness panel, Mark Ready, dashboard counts) deferred to Phase 4. |
| 061 | 2026-04-24 | Phase 4.0 foundations -- ADMIN inheritance + seed variety: removed `isExact` helper from `frontend/src/lib/permissions.ts` so ADMIN inherits VENDOR-side actions (`canAcceptRejectPO`, `canCreateInvoice`, `canSubmitInvoice`, `canPostMilestone`). First commit of `backend/src/seed.py` into git (previously untracked); expanded `_make_activity_log` from 8 to 19 rows covering all 8 named event types and derived `(category, target_role)` from `EVENT_METADATA` to prevent drift. New `backend/tests/test_seed.py` with 6 tests asserting 10 variety thresholds (vendors/types/countries, POs/statuses, invoices/statuses, milestones/stages, activity/events, users/roles). Pre-existing seed issues (non-deterministic UUIDs, `_PAYMENT_TERMS_CYCLE` dead branches, module-level `random.seed`/`_NOW`) logged to backlog for future cleanup. 591 backend tests + 100 Playwright specs green. |
| 062 | 2026-04-24 | Phase 4.0 design-system scaffold: new `(nexus)` SvelteKit layout group (empty passthrough; AppShell deferred to iter 068), 22 new design tokens appended to `global.css` (`--surface-*`, `--text-sidebar*`, `--brand-accent`, `--button-solid-*`, `--dot-*` status dots, `--font-size-xs`/`--font-size-3xl`, `--letter-spacing-wide`, `--space-7`/`--space-20`, `--breakpoint-*`), `/ui-demo` route skeleton (populated by later iters). Additive only -- zero existing tokens or component rules modified. Incidentally resolves two pre-existing `--font-size-xs` consumers (dashboard, shipments) that had been falling through to browser defaults. 591 backend tests + 100 Playwright specs green. |
| 063 | 2026-04-24 | Phase 4.0 leaf primitives: seven Svelte 5 primitives under `frontend/src/lib/ui/` — Button (primary/secondary/ghost + disabled), StatusPill (green/blue/orange/red/gray tones + leading dot), ProgressBar (role=progressbar, clamped `$derived` value), Input, Select, DateInput (native `<input type="date">`), Toggle (button + aria-pressed, bindable pressed). All primitives use the `ui-<name>` outermost-class convention so scoped styles cannot collide with pre-revamp `global.css` rules (`.btn` / `.badge` / `.input` / `.select` / etc.). Pre-revamp `$lib/components/StatusPill.svelte` unchanged. New `frontend/tests/primitives.spec.ts` with 9 Playwright tests, wired through the `/ui-demo` gallery. Playwright mock order fixed (LIFO — catch-all must register before specific `/auth/me` mock). Code reviews caught `.btn:hover` opacity leak from pre-revamp on Button secondary/ghost; fixed via `ui-btn` rename. 591 backend tests + 109 Playwright specs green. |
| 064 | 2026-04-24 | Phase 4.0 composite primitives: five more Svelte 5 primitives under `frontend/src/lib/ui/` — FormField (server-error contract with `role="alert"`, wraps child via snippet that exposes `{ invalid }`), PanelCard (section with title + subtitle + optional action slot + body), AttributeList (definition-list grid of label/value rows), FormCard (form surface with Cancel + Submit footer buttons, uses Button primitive), KpiCard (label + big value + tone-colored delta chip). Input primitive extended with `aria-invalid={invalid || undefined}` (omit-when-false per W3C) so FormField's invalid state surfaces through ARIA. `ui-<name>` class convention applied to all five; critical `.card` → `.ui-form-card` rename prevents double-styling from pre-revamp `global.css` `.card`. `primitives.spec.ts` grows by 5 tests (total 14). 591 backend tests + 114 Playwright specs green. |
| 065 | 2026-04-24 | Phase 4.0 display + state primitives: six more Svelte 5 primitives under `frontend/src/lib/ui/` — Timeline (ordered steps with done/current/upcoming state, colored marker), ActivityFeed (dot + primary + secondary entry rows using `--dot-*` tokens), LoadingState (role=status spinner, `prefers-reduced-motion` fallback disables rotation), EmptyState (centered title + description + optional action snippet), ErrorState (alert banner wrapping iter-063 Button as Retry, `role="alert"`), ErrorBoundary (Svelte 5 `<svelte:boundary>` wrapper falling back to ErrorState; does not expose error to user to avoid leaking stack traces). ErrorBoundary installed but not rendered on `/ui-demo` — will be wired into `(nexus)/+layout.svelte` at iter 068. All use `ui-<name>` class convention (ErrorBoundary has no class — it's a wrapper). `primitives.spec.ts` grows by 5 tests (total 19; ErrorBoundary not unit-tested). 591 backend tests + 119 Playwright specs green. |
| 066 | 2026-04-24 | Phase 4.0 table + header primitives: three more Svelte 5 primitives under `frontend/src/lib/ui/` — DataTable (generic over `T extends { id: string }`, server-driven pagination contract `{ page, pageSize, total, onPageChange }`, Prev/Next Buttons with `Page X of Y`, clickable rows with tabindex/Enter/Space keyboard), PageHeader (H1 + optional subtitle + optional action snippet), DetailHeader (back link + title + optional subtitle + optional statusPill snippet). DataTable `<table>` has no class attribute to avoid collision with pre-revamp global `.table` rule. `primitives.spec.ts` grows by 3 tests (total 22). 591 backend tests + 122 Playwright specs green. |
| 067 | 2026-04-24 | Phase 4.0 sidebar foundation: `frontend/src/lib/ui/sidebar-items.ts` (explicit `Record<UserRole, SidebarItem[]>` map decoupled from `permissions.ts`) + `Sidebar.svelte` primitive consuming it via `$derived`, using `$app/state`'s `page.url.pathname` for `aria-current="page"`. Per-role items locked after brainstorm: ADMIN gains Users nav (future `/users` page, 404 until built); FREIGHT_MANAGER gains Invoices nav (OpEx-only page scoping backlog'd for iter 071+); SM/VENDOR/QUALITY_LAB/PROCUREMENT_MANAGER unchanged; Shipments deferred. Sidebar visibility intentionally decoupled from `canView*` permission helpers — nav visibility and page capability are separate concerns. `primitives.spec.ts` grows by 2 Sidebar tests (total 24); new `sidebar-items.spec.ts` with 6 role-matrix tests. 591 backend tests + 130 Playwright specs green. |
| 068 | 2026-04-24 | Phase 4.0 shell: `TopBar.svelte` (hamburger toggle + optional breadcrumb hidden on mobile via `@media max-width: 767px` + embedded pre-revamp NotificationBell + optional userMenu snippet; search omitted in 4.0) and `AppShell.svelte` (composes Sidebar + TopBar + iter-065 ErrorBoundary; desktop 240px + 1fr grid; mobile ≤768px off-canvas drawer at `min(280px, 70vw)` with `translateX` + `visibility:hidden` transition + tap-to-dismiss overlay). Separate `/ui-demo/shell` route hosts the AppShell preview. All three Phase 4.0 brainstorms (Tasks 18, 20, 22) resolved from Lovable mock screenshots captured at 1440/768/390 viewports via Playwright MCP. Chrome scope = Option B: mock visual chrome + real routes. Retrofits to existing primitives: `sidebar-items.ts` returns `SidebarSection[]`; `Sidebar.svelte` gains section headers + `roleLabel?: string` + `footer?: Snippet`; `KpiCard.svelte` gains `icon?: Snippet` slot. `primitives.spec.ts` grows to 31 tests. 591 backend + 140 Playwright green. |
| 069 | 2026-04-24 | Phase 4.0 shell finish: `UserMenu.svelte` (pill with avatar + name + role stacked on desktop, collapses to avatar + chevron on mobile via `@media max-width: 767px`; dropdown with Log out; `import.meta.env.DEV`-gated Switch role placeholder for future dev-store wiring; logout swallows API errors and always redirects to `/login`). `redirects.ts` + spec (empty registry in Phase 4.0; `resolveRedirect` substitutes `:param` tokens via named-capture regex). `(nexus)/_smoke/+page.svelte` sentinel route mounts AppShell + UserMenu with `page.data.user`-derived role/name/roleLabel. `nexus-shell.spec.ts` permanent E2E test covers ADMIN-full-nav, VENDOR-no-Vendors, and 401-redirect invariants, scoping assertions to `ui-appshell-sidebar` testid to avoid colliding with pre-revamp nav links. `primitives.spec.ts` grows to 33 tests; new `redirects.spec.ts` (3) and `nexus-shell.spec.ts` (3). 591 backend + 148 Playwright green. |
| 070 | 2026-04-24 | **Phase 4.0 close.** Adds @axe-core/playwright and runs AA accessibility scan on `/ui-demo` and `/_smoke` (zero violations / fixed inline — see iter doc). Scratch 390px + 1024px screenshots captured locally for visual verification but not committed (per CLAUDE.md scratch-test rule). Work-log summary updated to mark Phase 4.0 complete. 591 backend + 150 Playwright green. Branch `ux-changes` ready for PR to main. |
| 071 | 2026-04-26 | **Phase 4.1 dashboard for ADMIN+SM.** New `frontend/src/routes/(nexus)/dashboard/+page.svelte` consuming new `GET /api/v1/dashboard/summary` (ADMIN global, SM PROCUREMENT-scoped, others empty payload). 4 KPIs (Pending POs, Awaiting acceptance, In production, Outstanding A/P) + Awaiting-acceptance panel + Recent activity panel via Phase 4.0 primitives (AppShell, KpiCard, ActivityFeed, PanelCard, EmptyState). Other roles get a thin placeholder. Pre-revamp `frontend/src/routes/dashboard/+page.svelte` and its specs deleted. Sidebar/permissions matrix patched: FREIGHT_MANAGER drops POs, VENDOR adds Products, PROCUREMENT_MANAGER promoted to SM-equivalent (read-only). Root layout `isRevampRoute` extended for `/dashboard*`. New `nexus-dashboard.spec.ts` (6 tests). Test fixture updates in `auth-flow.spec.ts`, `notification-bell.spec.ts`, `role-rendering.spec.ts`, `invoice-list.spec.ts` per route migration. 595 backend + 145 Playwright green. |
| 072 | 2026-04-26 | **Phase 4.1 dashboard polish.** KPI cards 1-3 now show USD value alongside count via `KpiCard.delta` chip (`pending_pos_value_usd`, `awaiting_acceptance_value_usd`, `in_production_value_usd` added to backend response). Recent activity panel curated: `_DASHBOARD_EXCLUDED_EVENTS` frozenset filters line-level negotiation events + force transitions + convergence + email-send failures; `target_role=user.role.value` passed to `list_recent` for SM (ADMIN sees all). Activity rows now render as `<a>` links to `/po/[id]` or `/invoice/[id]` when the role has `canViewPOs`/`canViewInvoices`, else as non-clickable `<span>`. `TopBar` hamburger hidden at `≥769px` viewports. Backlog: sidebar collapse mode, ACTION_REQUIRED row pinning. 595 backend + 146 Playwright green. |
| 073 | 2026-04-26 | **FREIGHT_MANAGER dashboard.** New FM branch on `(nexus)/dashboard/+page.svelte` replacing the placeholder. 4 KPIs (Ready batches, Shipments in flight, Pending invoices + USD, Docs missing) + 3 panels (Ready batches, Pending invoices, Recent activity). Backend `/dashboard/summary` extended with optional `fm_kpis: FmKpis \| None`, `fm_ready_batches`, `fm_pending_invoices` fields (additive — ADMIN/SM unchanged in shape and content). FM-specific KPI 1 (Ready batches) uses existing `READY_TO_SHIP` milestone as proxy until the new `READY_FOR_SHIPMENT` milestone ships in a future iter. KPI 4 (Docs missing) reads from `shipment_document_requirements` PENDING rows. `canViewPOs` widened to include FREIGHT_MANAGER for ready-batch click-through (sidebar visibility unchanged, decoupled per iter 067). Backlog: `READY_FOR_SHIPMENT` milestone, vendor → FM mapping, OpEx/Freight invoice approval routing (SM owns procurement only), shipment seed extension. 596 backend + 149 Playwright green. |
| 074 | 2026-04-26 | **Shipment booking + READY_FOR_SHIPMENT milestone rename.** `ProductionMilestone.READY_TO_SHIP` renamed to `READY_FOR_SHIPMENT` (5-stage sequence preserved); one-shot `init_db` UPDATE migrates existing rows. `ShipmentStatus` extended with `BOOKED` and `SHIPPED`; new sequence: DRAFT > DOCUMENTS_PENDING > READY_TO_SHIP > BOOKED > SHIPPED. New `Shipment.book_shipment(carrier, booking_reference, pickup_date)` and `Shipment.mark_shipped()` methods with status guards and non-empty validation. Schema adds carrier/booking_reference/pickup_date/shipped_at columns (all nullable). New endpoints: `POST /shipments/{id}/book` and `POST /shipments/{id}/ship` (ADMIN+SM+FREIGHT_MANAGER). New activity events `SHIPMENT_BOOKED` + `SHIPMENT_SHIPPED` (LIVE, target_role=SM). FM dashboard `_SHIPMENT_IN_FLIGHT_STATUSES` now includes BOOKED (excludes SHIPPED). Seed reserves first accepted PO at READY_FOR_SHIPMENT with no shipment (drives ready_batches KPI); remaining shipments span all 5 statuses with carrier metadata on BOOKED + SHIPPED rows. Frontend types updated (no booking UI yet — backlog). 610 backend + 149 Playwright green. |
| 075 | 2026-04-26 | **PROCUREMENT_MANAGER dashboard parity.** PM now receives the SM dashboard payload: same `procurement_only` scoping, same KPI grid, same panels, with the activity feed filtered by `target_role=PROCUREMENT_MANAGER`. Backend renames `_ADMIN_OR_SM` to `_DASHBOARD_FULL_LAYOUT_ROLES` and adds PM; `TargetRole` enum gains `PROCUREMENT_MANAGER` so activity rows targeted at PM no longer raise `ValueError` in `_row_to_entry`. Frontend `isFullLayout` extended to PM. Backlog: fan-out of activity events to PM (today the EVENT_METADATA default routes everything to SM, so PM's activity panel is near-empty on a fresh seed). 612 backend + 150 Playwright green. |
| 076 | 2026-04-27 | **`/po` list revamp (Phase 4.2 Tier 1).** Replaces inline filter bar / table / pagination / bulk toolbar with the new Phase 4.2 component set in `frontend/src/lib/po/`: `PoListFilters`, `PoListBulkBar`, `PoStatusPills`, `PoListTable`, `PoListPagination`. Adds `marketplace` filter (4-enum) + `canBulkPO` permission + responsive layout (table at desktop, stacked cards at 390px) + dashboard-grade empty/loading/error states (`LoadingState` overlay preserves prior rows on filter refetch per G-06). Old `frontend/src/routes/po/+page.svelte` removed; route now served from `(nexus)/po/+page.svelte` so it inherits AppShell + Sidebar + TopBar (matches iter 071 dashboard precedent). `isRevampRoute` extended for `/po*`. Existing `po-list.spec.ts` migrated to testid selectors (`po-filter-*`, `po-bulk-bar`, `po-bulk-action-{action}`, `po-table`, `po-row-{id}`, `po-pagination`, `po-status-partial`, `po-list-loading`, `po-page-header-action`); ~30 brittle class/tag assertions retired. `po-negotiation-events.spec.ts` and `role-rendering.spec.ts` updated for new selectors (status pill markup + New PO is now a button not a link). New CLAUDE.md selector policy bans class/tag/bare-text selectors in new tests; primitive `label`/`aria-label` plumbing queued in backlog. 612 backend + 156 Playwright green. |
| 077 | 2026-04-29 | **`/po/[id]` detail revamp (Phase 4.2 Tier 2).** Page moved from `frontend/src/routes/po/[id]/+page.svelte` to `(nexus)/po/[id]/+page.svelte` so it inherits AppShell + Sidebar + TopBar. Replaces inline header card + bottom button row + inline mark-advance-paid block with four reusable components from `frontend/src/lib/po/`: `PoDetailHeader` (back link + title + vendor/issued-date subtitle + status pill), `PoActionRail` (one role-aware rail, inline at desktop ≥1024px and sticky bottom at <1024px, primary actions visible + overflow menu for the rest), `PoAdvancePaymentPanel` above Line Items surfacing the Post-Acceptance Gate state from `can_modify_post_acceptance` (open/closed copy with trigger event), and `PoCertWarningsBanner` between header and first PanelCard sourced from `POSubmitResponse.cert_warnings` with in-memory dismissal. VENDOR sees a status-only Advance panel; PROCUREMENT_MANAGER, FREIGHT_MANAGER, QUALITY_LAB see Download PDF only. Download PDF stays in the overflow at every viewport with "Modified" tag when `round_count > 0`. Legacy line items, milestone timeline, invoices, rejection history, and activity sections stay pre-revamp shape (Tier 3+ scope). `isRevampRoute` extended to a prefix match so `/po`, `/po/`, `/po/{id}`, `/po/new`, and `/po/{id}/edit` all hide the pre-revamp top nav. 15 new Playwright tests in `frontend/tests/po-detail.spec.ts` cover the role × status matrix plus advance-gate copy and cert-banner dismissal. Resolves G-13, G-14, G-17, G-20, G-23. 612 backend + 171 Playwright green. |
| 079 | 2026-04-29 | **Dev quick-login (one-click session for seeded users).** New `POST /api/v1/auth/dev-login` and `GET /api/v1/auth/dev-users` in `backend/src/routers/auth.py`, both gated by a `_dev_auth_enabled()` helper checking `DEV_AUTH=1`. When the flag is unset both endpoints raise `HTTPException(404, "Not found")` matching FastAPI's default 404 shape, so the surface is indistinguishable from a non-existent route in production. `dev-users` returns ACTIVE users sorted alphabetically by username (alice/bob/carol/dave/erin/frank); `dev-login` looks up by username, validates ACTIVE status, and calls `_set_session_cookie` on success. Frontend adds `getDevUsers()` (swallows 404 → null) and `devLogin(username)` to `frontend/src/lib/auth.ts`; `/login` page calls `getDevUsers()` on mount and renders a "Dev quick-login" row with one button per user when the response is a list, hidden when null. Click triggers `devLogin` then redirects honoring `?redirect=` query param. Replaces the earlier GET `/dev-login` from iter 031 which returned JSON without redirect. 7 new pytest in `test_auth_dev_login.py`/`test_auth_dev_users.py` cover the env-gate matrix + active-user filtering + session creation; 4 new Playwright in `frontend/tests/dev-login.spec.ts` cover the login-page surfacing, click-to-redirect, and `?redirect=` honoring. 619 backend + 175 Playwright green. |
| 080 | 2026-04-29 | **Primitive `label` / `aria-label` plumbing (Phase 4.x cleanup).** Retrofits 15 primitives in `frontend/src/lib/ui/` to expose accessible names via three patterns: control primitives (`Input`, `Select`, `DateInput`) gain an `ariaLabel?: string` prop forwarded to `aria-label`; list/table containers (`ActivityFeed`, `Timeline`, `AttributeList`, `DataTable`) gain a `label?: string` prop on the root element with sensible defaults ("Activity feed", "Timeline"); titled containers (`PanelCard`, `KpiCard`, `FormCard`, `DetailHeader`, `PageHeader`) generate a `crypto.randomUUID()` id at component init and wire `aria-labelledby` to the existing visible heading. `Sidebar` sections get the same uuid + `aria-labelledby` treatment for nav grouping. `UserMenu` trigger defaults `ariaLabel="Open user menu"`; the dropdown gets static `aria-label="Account actions"`. `DetailHeader` and `PageHeader` gain explicit `role="banner"` so they resolve outside top-level document scope. `StatusPill` is comment-only (visible text already serves as implicit name). Components with implicit names stay untouched: `FormField` (wraps with `<label>`), `Button` (text is name), `Toggle` (`label` already wired), plus `EmptyState`, `AppShell`, `TopBar`, `ProgressBar`, `LoadingState`, `ErrorState`, `ErrorBoundary`. CLAUDE.md gains a "Primitive accessible-name convention" section under the Selector policy codifying the three patterns so new primitives follow them from day one. 15 new Playwright tests in `frontend/tests/primitives.spec.ts` cover one role/label query per retrofitted primitive (173 → 188 Playwright total). 619 backend + 188 Playwright green. |
| 081 | 2026-04-29 | **`/po/[id]` line negotiation revamp (Phase 4.2 Tier 3).** Replaces five iter-057 components under `frontend/src/lib/components/` (`LineNegotiationRow`, `ModifyLineModal`, `LineDiff`, `EditHistoryTimeline`, `SubmitResponseBar`) with six new components under `frontend/src/lib/po/` (`PoLineNegotiationTable`, `PoLineNegotiationRow`, `PoLineModifyModal`, `PoLineDiff`, `PoLineEditHistoryTimeline`, `PoSubmitResponseBar`). Each line renders as a self-contained card with status pill, action row (Modify/Accept/Remove + Force Accept/Force Remove for SM at round 2), and a single "View changes" toggle revealing both diff and history. Submit-response bar uses `position: sticky` (not fixed) so it scrolls inside AppShell content area; `env(safe-area-inset-bottom)` padding handles iOS. Mobile (390px) actions stack two-per-row via `flex: 1 1 calc(50% - gap)`; lines collapse to vertical stacking. Status pill mapping: PENDING→gray, MODIFIED_BY_VENDOR→blue, MODIFIED_BY_SM→orange, ACCEPTED→green, REMOVED→red. Modify modal validates non-empty UoM/Description/HS-code/Country and routes qty=0 to a "Remove line" submit. Backend domain methods unchanged. Tier 3 visual mock at `/ui-demo/po-line-negotiation` shipped ahead of the iter (commit d5abea1) with toggleable role × round selectors covering all matrix cells. 8 iter-057 specs in `po-negotiation.spec.ts` migrated to the new testid contract; 14 new specs landed inline; one selector in `po-lifecycle.spec.ts` and two in `role-rendering.spec.ts` migrated as scope expansion (legacy testids deleted with the components). Resolves G-15 + G-16. Backlog opens: round indicator slot on `PoDetailHeader` (G-15 #6 deferred), button-group primitive, `Dialog` primitive. 619 backend + 206 Playwright green. |
| 083 | 2026-04-29 | **`/po/[id]` finishing pass (Phase 4.2 Tier 5).** Replaces the last four pre-revamp blocks on `(nexus)/po/[id]` with `PoMetadataPanels` (four `PanelCard` + `AttributeList` panels for Trade Summary / Buyer / Vendor / Trade Details + a Terms & Conditions panel with free-text body), `PoRejectionHistoryPanel` (`PanelCard` wrapping latest-first `<article>` rows; G-25), `PoInvoicesPanel` (`PanelCard` hand-rolling a `<table>` because DataTable's `render` returns `string \| number` only — same precedent as `PoListTable`; subtitle shows remaining-quantity total for PROCUREMENT, "OPEX — single invoice" copy for empty OPEX; G-24), and `PoActivityPanel` (`PanelCard` + Phase 4.0 `ActivityFeed` primitive with client-side "Show more" pagination over a single `fetchActivityForEntity` fetch + `LoadingState` + `EmptyState`; G-21). New `frontend/src/lib/po/event-labels.ts` exports `EVENT_LABELS` (19 events, copied from legacy `ActivityTimeline`) + `categoryToTone(category)` mapping LIVE→blue / ACTION_REQUIRED→orange / DELAYED→red so future PO event surfaces share one source. Page CSS shrinks ~35 lines (`.info-grid`, `.rejection-record`, `.terms-text`, etc. retired). Legacy `ActivityTimeline.svelte` and `components/StatusPill.svelte` files retained for `invoice/[id]` until that page revamps. `/ui-demo/po-finishing` mock route ships toggleable status × rejection × invoices × activity matrix using a new `mockEntries?` bypass prop on `PoActivityPanel`. 16 new Playwright (`po-detail.spec.ts`) + 4 migrated legacy specs (`activity-timeline.spec.ts`, `po-negotiation-events.spec.ts`) on the new `po-activity-feed` testid contract. 629 backend + 239 Playwright green. |
| 085 | 2026-04-29 | **`/po/new` + `/po/[id]/edit` POForm retrofit (Phase 4.2 G-08 + G-09 + G-10 + G-11 + G-12 + G-26 + G-27).** Both routes move from `frontend/src/routes/po/*` to `(nexus)/po/*` so they inherit AppShell + Sidebar + TopBar. Legacy `frontend/src/lib/components/POForm.svelte` (532 lines, native `<input class="input">` + `<select class="select">`) replaced by [frontend/src/lib/po/PoForm.svelte](frontend/src/lib/po/PoForm.svelte) using Phase 4.0 `PanelCard` + `FormField` + `Input` + `Select` + `DateInput` + `Button` primitives. New `mode: 'create' \| 'edit-draft' \| 'edit-revise'` prop drives field disability: PO Type `Select` disabled on edit (G-09 #5 + G-27 #2); Marketplace `Select` hidden entirely on OPEX (G-09 #4); Vendor `Select` disabled until PO Type chosen, with inline `FormField` hint copy "Vendor cleared because it does not match the selected PO type." when a po_type switch invalidates the current vendor (G-09 #1, #2). Payment Terms options append " — advance required" suffix when `has_advance` is true (G-11 #2). Reference-data `Select`s show `code — label` closed-state parity with the PDF Reference Label resolver (G-11 #3). Dirty-tracking via `JSON.stringify` snapshot diff (initial captured with `untrack`); pressing Cancel while dirty opens an in-page discard modal (`po-form-discard-modal` testid) with "Keep editing" + "Discard" Buttons; pristine cancel navigates immediately. Cancel destination branches: `/po` for create, `/po/[id]` for edit (G-12 #1). Sticky-on-mobile footer with `env(safe-area-inset-bottom)` padding (G-12 #4). Edit page now accepts BOTH DRAFT and REJECTED per ddd-vocab "PO mutable only in Draft and Rejected" — a long-standing gap; legacy `/po/[id]/edit` returned "Only rejected POs can be edited." for DRAFT. DRAFT submit label = "Save Draft"; REJECTED submit label = "Save & Revise"; non-editable statuses render a not-editable panel with a Back to detail Button (G-26 #1, #3). REJECTED edit renders `PoRejectionHistoryPanel` above the form so the SM sees what to address (G-26 #5). Internal line state shape diverges from `LineItemInput`: `LineFormState` uses string-typed `quantity` + `unit_price` so the `Input` primitive's string-only `value` contract two-way binds; reconstructed at submit via `Number.parseInt`. G-27 #4 nominal "one FormCard with PanelCard sections" deviates: form renders four `PanelCard`s + custom sticky footer instead of `FormCard`'s single-title + non-sticky-footer shape, since `FormCard`'s constraints would either duplicate the page H1 or break G-12 #4's mobile-sticky requirement. Pre-revamp `/po*` carve-out comment in [frontend/src/routes/+layout.svelte](frontend/src/routes/+layout.svelte) removed; existing `startsWith('/po/')` check in `isRevampRoute` already covers every PO route uniformly. 8 `/po/new` tests in `po-lifecycle.spec.ts` + 1 in `vendor.spec.ts` migrated from `#field` / `input[placeholder=...]` / `.error-message` selectors to the new `po-form-*` testid namespace. 18 new specs in `po-form.spec.ts` cover AppShell mount, po_type / marketplace dependency, vendor-clear hint, payment-terms `has_advance` suffix, HS code error display + submit gating, Add Line, valid POST, dirty-state Cancel modal, REJECTED + DRAFT edit happy paths, ACCEPTED not-editable branch, and pristine-edit Cancel. 696 backend (no change) + 275 Playwright (+18). |
| 094 | 2026-04-30 | **Phase 4.5 Tier 4 — `/products/[id]/edit` certificates panel (iter-040 cert UI fold-in).** Last cert-UI piece from the original iter-040 plan: list + create + PDF upload on the product edit page. New [frontend/src/lib/product/ProductCertificatesPanel.svelte](frontend/src/lib/product/ProductCertificatesPanel.svelte): `PanelCard` "Certificates" with SM-only "Add Certificate" trigger; trigger disabled when the product has zero qualifications (with a hint line "Assign a qualification first to upload certificates against it."); `EmptyState` ("No certificates uploaded yet.") when zero certs; certs grouped by `qualification_type_id` (label = `${qt.name} — ${qt.target_market}` resolved from the product's qualifications array); per-row Phase 4.0 `StatusPill` (PENDING → gray, VALID → green, EXPIRED → red); per-row Upload PDF `Button` (and hidden `<input type="file" accept="application/pdf">`) gated on `document_id === null` + canManage. Add flow: 8 fields wrapped in `FormField` (Qualification `Select`, Target Market `Input`, Cert Number `Input`, Issuer `Input`, Testing Lab `Input`, Issue Date `DateInput`, Expiry Date `DateInput`, Test Date `DateInput`); target_market defaults to the selected qualification's target_market via a `$effect` on `qualification_type_id` change (Phase 4.0 `Select` exposes `options` only — no `onchange`); `wasAdding` `$effect` auto-closes the form on success identical to iter 093. Backend `CertificateListItem` DTO extended with `document_id: str | None` so the panel can decide upload-button visibility from the list payload alone. New API client functions in [frontend/src/lib/api.ts](frontend/src/lib/api.ts): `listCertificates(productId, targetMarket?)` GETs `/api/v1/certificates/?product_id=...`, `createCertificate(input)` POSTs `/api/v1/certificates/`, `uploadCertificateDocument(certId, file)` POSTs multipart `file` to `/api/v1/certificates/{id}/document` (mirrors iter 084 `PoDocumentsPanel.handleUpload`). New types in [frontend/src/lib/types.ts](frontend/src/lib/types.ts): `CertificateStatus`, `CertificateListItem`, `Certificate`, `CertificateCreateInput`. PDF client-side validation: PDF MIME + 10MB ceiling, errors surfaced per-row in a `Record<string, string>` keyed by cert id (parity with iter 084's PoDocumentUploadDialog). Edit (PATCH metadata), approve (PATCH status=VALID), and dashboard expiry alerts deferred to follow-up iters — same split rhythm as iter 092 → 093. CERT_UPLOADED activity is recorded server-side but not surfaced on this page (no product-scoped activity panel yet). 8 new permanent specs in `product.spec.ts` (342 → 350): empty-state mount, marketplace grouping with three status tones (PENDING/VALID/EXPIRED), trigger reveals form, submit inserts row + auto-close, server error inline, blocks-submit-when-required-fields-empty, upload button hidden on certs with `document_id`, client-side oversize rejection (>10MB) without firing the request. Existing `'product edit form shows existing manufacturing_address'` spec broke on the new fetch in `onMount`; fixed by adding `**/api/v1/certificates**` to the global `beforeEach` mock alongside the existing packaging-specs catch-all. 727 backend (no domain change; one-line DTO add) + 350 Playwright (+8). |
| 093 | 2026-04-29 | **Phase 4.5 Tier 3b — `/products/[id]/edit` packaging specs panel.** Retires the last legacy block on the edit page. New [frontend/src/lib/product/ProductPackagingSpecsPanel.svelte](frontend/src/lib/product/ProductPackagingSpecsPanel.svelte): `PanelCard` "Packaging Specs" with SM-only "Add Spec" trigger in the action snippet; `EmptyState` ("No packaging specs defined yet.") when zero specs; specs grouped by marketplace with sub-headings; per-row Phase 4.0 `StatusPill` (PENDING → gray, COLLECTED → green) + SM-only Delete `Button` gated on `status === 'PENDING'`. Add flow: button toggles an inline form (4 fields — `FormField` + `Input` for marketplace + spec_name + description, native `<textarea>` for requirements_text since no Phase 4.0 textarea primitive yet, same gap as iter 087/089/091/092); submit + cancel via Phase 4.0 `Button`. Auto-close-on-success: `wasAdding` `$state` + `$effect` pair watches `adding` flip false-after-true with no `addError` — simpler than threading a success callback up through the parent. Delete uses native `window.confirm` (legacy parity — no Phase 4.0 confirm dialog primitive). `(nexus)/products/[id]/edit/+page.svelte` drops 70 lines of inline `<div class="section card">` markup + supporting `<style>` rules + `showAddSpec`/`newMarketplace`/`newSpecName`/`newDescription`/`newRequirementsText`/`addingSpec`/`addSpecError`/`specsByMarketplace` local state; mounts `<ProductPackagingSpecsPanel>` after `ProductQualificationsPanel` with `on_add_spec` / `on_delete_spec` callbacks wrapping the existing `createPackagingSpec` / `deletePackagingSpec` API calls. With this iter the entire `/products/*` namespace renders under (nexus) on Phase 4.0 with zero transitional markup. PackagingSpecStatus expands de facto to PENDING + COLLECTED via the panel's `statusTone` map even though the frontend type still narrows to `'PENDING'`; the COLLECTED branch renders correctly because StatusPill takes a string label, not a typed enum. Existing iter-092 setupEditPage helper had its packaging-specs mock URL corrected from `/api/v1/products/{id}/packaging-specs` to `/api/v1/packaging-specs/**` (matches `listPackagingSpecs` in `api.ts:458`). 7 new permanent specs in `product.spec.ts` (335 → 342): empty-state mount, marketplace grouping with status pills, add flow trigger→form→submit→row-appears + auto-close, add server error inline, delete-confirm-accepted removes row, delete-confirm-dismissed keeps row + skips DELETE, delete button hidden on COLLECTED. 696 backend (no change) + 342 Playwright (+7). |
| 092 | 2026-04-29 | **Phase 4.5 Tier 3a — `/products/[id]/edit` details + qualifications.** Largest pre-revamp page (436 lines) split per the iter discipline: 092 ships the details form + qualifications panel; iter 093 takes packaging specs; iter 094 takes the iter 040 cert UI fold-in. Two new components in [frontend/src/lib/product/](frontend/src/lib/product/): `ProductEditForm` (Phase 4.0 `PanelCard` "Product Details" with vendor + part_number rendered as readonly hand-rolled spans with explicit testids — `AttributeList` rejects per-row testids — plus editable `Input` (description) + native `<textarea>` (manufacturing_address) for SM/ADMIN; view-only branches scaffold a future read-only mode that the current `+page.ts` redirect doesn't allow), `ProductQualificationsPanel` (`PanelCard` + `EmptyState` when empty, list of rows with `StatusPill tone="blue"` showing target_market, SM-only Add row with `Select` + `Button` filtering already-assigned types out of the dropdown). New `(nexus)/products/[id]/edit/+page.{svelte,ts}`; legacy `frontend/src/routes/products/[id]/edit/+page.{svelte,ts}` deleted (and the entire `frontend/src/routes/products/` legacy tree retires). The packaging specs block stays inline on the new (nexus) shell as legacy markup with all its existing handlers — temporarily ugly but functionally unchanged — until iter 093 replaces it with `ProductPackagingSpecsPanel`. `isRevampRoute` widened from `pathname === '/products/new'` to `pathname.startsWith('/products/')`; the entire `/products/*` namespace now renders under (nexus). Existing `'product edit form shows existing manufacturing_address'` spec migrated from `#manufacturing_address` id to `product-edit-manufacturing-address` testid. 8 new permanent specs cover AppShell mount, vendor + part_number readonly, save+redirect, Cancel without save, qualifications list, empty state, add flow (POST to `/qualifications` with `{ qualification_type_id: ... }` body — initial test mock targeted the wrong URL shape and was fixed), remove flow. 696 backend (no change) + 335 Playwright (+8). |
| 091 | 2026-04-29 | **Phase 4.5 Tier 2 — `/products/new` create form revamp.** Route moves under `(nexus)` so it inherits AppShell + Sidebar + TopBar. New [`frontend/src/lib/product/ProductCreateForm.svelte`](frontend/src/lib/product/ProductCreateForm.svelte) mirrors iter 089's `VendorForm` shape exactly: Phase 4.0 `PanelCard` ("Product Details") + `FormField` + `Input` + `Select` + native `<textarea>` for manufacturing_address (no Phase 4.0 textarea primitive yet). Sticky-on-mobile footer with `env(safe-area-inset-bottom)` padding; static footer at >=768px. Validation: vendor_id required, part_number required-and-trimmed. The legacy 409-conflict detection (`err.message.includes('409')` + the user-facing copy "A product with this part number already exists for this vendor.") carries over verbatim — the backend's `(vendor_id, part_number)` uniqueness constraint from iter 029 is unchanged. Legacy `frontend/src/routes/products/new/+page.{svelte,ts}` deleted; `+page.ts` redirect-on-no-permission moved verbatim. `isRevampRoute` extended with `pathname === '/products/new'` exact match — cannot widen to `/products/` prefix yet because `/products/[id]/edit` (iter 092) still renders legacy chrome. The 2 existing `/products/new` specs migrated from `#vendor_id` / `#part_number` / `#manufacturing_address` id selectors and `getByRole('button', { name: 'Create Product' })` to the new testid namespace (`product-form-vendor`, `product-form-part-number`, `product-form-manufacturing-address`, `product-form-submit`). 5 new permanent specs cover AppShell mount, vendor-required validation, part-number-required validation, 409 conflict inline message, and Cancel-returns-to-/products. 696 backend (no change) + 327 Playwright (+5). |
| 090 | 2026-04-29 | **Phase 4.5 Tier 1 — `/products` list revamp.** Route moves under `(nexus)` so it inherits AppShell + Sidebar + TopBar (precedent: iter 086 / 089). Two new components in [frontend/src/lib/product/](frontend/src/lib/product/): `ProductListFilters` (Vendor select + Clear, mobile-collapsing panel mirrored from iter 089's `VendorListFilters`), `ProductListTable` (responsive desktop-table / mobile-cards split, shared `product-row-{id}` testids scoped via `product-table-desktop` / `product-table-mobile` parents per the iter 086 strict-mode collision pattern). Legacy `frontend/src/routes/products/+page.svelte` (138 lines) deleted; `+page.ts` redirect-on-no-permission moved verbatim. `isRevampRoute` extended with `pathname === '/products'` **exact match only** — pre-revamp `/products/new` and `/products/[id]/edit` keep the legacy top nav until iters 091/092. The legacy page read `product.requires_certification`, a field removed from `ProductListItem` in iter 036a (replaced by `qualifications: QualificationTypeListItem[]`); every row was silently rendering "No". The new list reads `qualifications.length` and surfaces it via Phase 4.0 `StatusPill` (tone `blue` + count when > 0; tone `gray` + "None" when 0) — fixing the dead-field bug as a side effect of the retrofit. `product.spec.ts` grows from 3 → 10 specs: 7 new permanent specs cover AppShell mount, list rendering, qualification pill copy on populated vs empty rows, vendor filter narrowing, empty-state when filter matches nothing, error+retry, and 390px mobile cards. The 3 existing specs (manufacturing_address create/edit) untouched since they target unrevamped routes. 696 backend (no change) + 322 Playwright (+7). |
| 089 | 2026-04-29 | **Phase 4.4 Tier 1 — `/vendors` + `/vendors/new` revamp.** Both routes move under `(nexus)` so they inherit AppShell + Sidebar + TopBar (precedent: iter 086 `/invoices`). Three new components in [frontend/src/lib/vendor/](frontend/src/lib/vendor/): `VendorListFilters` (Status + Vendor Type selects + Clear, mobile-collapsing panel mirrored from `PoListFilters`), `VendorListTable` (Phase 4.0 `StatusPill` ACTIVE→green / INACTIVE→gray + per-row Deactivate/Reactivate `Button`s; responsive desktop-table / mobile-cards split with shared `vendor-row-{id}` testids — strict-mode duplicates resolved via parent-testid scoping `vendor-table-desktop` / `vendor-table-mobile`, iter 086 precedent), `VendorForm` (Phase 4.0 `PanelCard` + `FormField` + `Input` + `Select` + native `<textarea>` for address/account_details — no Phase 4.0 textarea primitive yet, same gap as iter 087's `InvoiceDisputeModal`; sticky-on-mobile footer with `env(safe-area-inset-bottom)` padding mirroring iter 085's `PoForm`). New routes `(nexus)/vendors/+page.{svelte,ts}` and `(nexus)/vendors/new/+page.{svelte,ts}`; legacy `frontend/src/routes/vendors/` tree retired via `git rm`. `+page.ts` redirect-on-no-permission unchanged. `isRevampRoute` extended with `pathname === '/vendors'` exact + `pathname.startsWith('/vendors/')` prefix. Existing `frontend/tests/vendor.spec.ts` migrated end-to-end from `#name` / `tbody tr` / `page.fill('#country', ...)` legacy selectors to the testid surface (`vendor-form-name`, `vendor-form-country`, `vendor-form-submit`, `vendor-row-{id}`, `vendor-row-action-{id}`, `vendor-row-status-{id}`); 9 specs grew to 17 with 8 new permanent specs covering AppShell mount on both routes, filter narrowing by status, empty-state when filtered to nothing, error-state with retry, deactivate/reactivate action-button swap, mobile (390px) cards layout, name-required validation gating submit, and Cancel-returns-to-/vendors. The unrelated `'PO form prefills buyer fields with defaults'` spec at the bottom of `vendor.spec.ts` (historically misplaced — it tests `/po/new`) already used testids and stays intact. `role-rendering.spec.ts:355` (VENDOR redirect from `/vendors`) continues passing because the redirect happens at the load-function layer in `+page.ts` before any markup renders. 696 backend (no change) + 315 Playwright (+7). |
| 088 | 2026-04-29 | **Phase 4.3 Tier 3 — invoice create modal retrofit.** Replaces legacy `frontend/src/lib/components/CreateInvoiceDialog.svelte` (113 lines, `<table class="table">` + `<input class="qty-input">` + raw `.btn-primary`/`.btn-secondary`) with [frontend/src/lib/invoice/InvoiceCreateModal.svelte](frontend/src/lib/invoice/InvoiceCreateModal.svelte) on Phase 4.0 primitives (`Button`, `Input`). Responsive layout: desktop renders a `<table>` (testid `invoice-create-table`) with one `<tr>` per remaining line; <768px renders a `<ul>` of cards (testid `invoice-create-cards`) so the qty input keeps a thumb-friendly hit area. Both layouts share row testid `invoice-create-row-{part_number}` and qty input testid `invoice-create-qty-input-{part_number}` — strict-mode duplicate-testid collisions resolved via parent-testid scoping (iter 086 precedent). Quantities held in a `Record<string, string>` keyed by `part_number` to match Phase 4.0 `Input`'s string-only `bind:value` contract (same shape as `PoForm.LineFormState` in iter 085); default per row = `String(line.remaining)`. Submit clamps each value at `Math.min(parsed, line.remaining)` rather than relying on the native `max` attribute (more deterministic across browsers). Lines with `remaining === 0` render with the qty input disabled. Create button disabled until at least one line has `parsed > 0`. Root has `role="dialog"` + `aria-modal="true"` + `aria-labelledby` on a `crypto.randomUUID()`-keyed `<h2>` per iter 080 titled-container convention. `(nexus)/po/[id]/+page.svelte` updated: import swap and element swap; props (lines / onConfirm / onCancel) unchanged. Legacy file deleted via `git rm`. `frontend/src/lib/components/` still contains `NotificationBell.svelte` (consumed by `TopBar`) and `RejectDialog.svelte` (consumed by pre-revamp test fixtures); directory retirement is a Phase 4 close-out task. OPEX flow unchanged — `handleCreateOpexInvoice` still skips the modal and calls `createInvoice(poId)` with no line items. New `frontend/tests/invoice-create.spec.ts` with 8 specs covering modal mount + role=dialog, per-line row pre-fill at remaining, disabled-row for `remaining === 0`, all-zero Create button gating, Cancel without API fire, non-zero-only POST + nav to `/invoice/{id}`, clamp-at-submit for `99` → `7`, and 390px sticky-bottom mobile-cards layout. The 390px spec required clicking "Create Invoice" through the action rail's overflow menu (`po-detail-page-rail-mobile` wrapper) rather than `.first()` on `po-action-rail` — the inline rail is `display:none` at <768px so `.first()` resolves to a non-visible element. 696 backend (no change) + 308 Playwright (+8). Phase 4.3 invoice surface (list + detail + create) feature-complete on Phase 4.0 primitives. |
| 087 | 2026-04-29 | **Phase 4.3 Tier 2 — `/invoice/[id]` detail revamp.** Route moves from `frontend/src/routes/invoice/[id]/+page.svelte` (204-line pre-revamp surface using legacy `StatusPill` + `DisputeDialog` + `ActivityTimeline` from `frontend/src/lib/components/`) to [(nexus)/invoice/[id]/+page.svelte](frontend/src/routes/(nexus)/invoice/[id]/+page.svelte) so it inherits AppShell + Sidebar + TopBar (precedent: iter 077). Seven new components in [frontend/src/lib/invoice/](frontend/src/lib/invoice/): `InvoiceDetailHeader` (DetailHeader-backed back link to `/invoices` + title + vendor·PO·created subtitle + Phase 4.0 StatusPill), `InvoiceActionRail` (role-aware: VENDOR sees Submit on DRAFT; SM sees Approve+Dispute on SUBMITTED, Pay on APPROVED, Resolve on DISPUTED; everyone sees Download PDF — inline at desktop ≥768px, sticky-bottom at <768px with overflow opening upward), `InvoiceDisputeModal` (replaces legacy `DisputeDialog` — Phase 4.0 Button-backed with native textarea, validates non-empty trim), `InvoiceMetadataPanel` (PanelCard + AttributeList for Currency/Payment Terms/Subtotal/Created + separate PO link row), `InvoiceDisputeReasonPanel` (PanelCard, conditional on `status === 'DISPUTED'`), `InvoiceLineItemsPanel` (PanelCard wrapping responsive table-at-desktop / cards-at-mobile, hand-rolled because DataTable's `render` is `string \| number` only — same precedent as `PoListTable` and `PoInvoicesPanel`), `InvoiceActivityPanel` (PanelCard + Phase 4.0 ActivityFeed + client-side Show more pagination, mirror of `PoActivityPanel`). `isRevampRoute` extended with `pathname.startsWith('/invoice/')` (singular) — the existing `=== '/invoices'` exact match for the list stays. Shared event-label dictionary promoted from `frontend/src/lib/po/event-labels.ts` to [frontend/src/lib/event-labels.ts](frontend/src/lib/event-labels.ts) so both `PoActivityPanel` and `InvoiceActivityPanel` consume one canonical module. [`PoInvoicesPanel`](frontend/src/lib/po/PoInvoicesPanel.svelte) migrated from legacy `$lib/components/StatusPill.svelte` to Phase 4.0 `$lib/ui/StatusPill.svelte` so all three legacy components — `StatusPill`, `DisputeDialog`, `ActivityTimeline` — could be deleted in the same iter. PROCUREMENT_MANAGER + FREIGHT_MANAGER see Download PDF only; QUALITY_LAB never reaches `/invoice/:id` because `canViewInvoices` excludes them. PDF action uses `window.open` — Playwright PDF test instruments a `window.open` shim via `page.addInitScript` to capture the requested URL synchronously rather than depending on popup load timing (the brittleness was hit during green-up). 19 new specs in `frontend/tests/invoice-detail.spec.ts` cover the role × status matrix, dispute lifecycle (open/cancel/post), dispute-reason panel visibility, metadata + line items + activity panels, Show more pagination, PDF download, and the sticky-rail at 390px. 3 invoice-detail specs in `frontend/tests/activity-timeline.spec.ts` migrated from `.timeline-entry` / `.timeline-dot` / `.timeline` legacy class selectors to the new `invoice-activity-feed` testid + `.dot.orange` tone-class assertion. Existing `'invoice detail page shows Download PDF button'` spec at the bottom of `invoice-list.spec.ts` deleted in favor of the dedicated detail file. 696 backend (no change) + 300 Playwright (+18). Phase 4.3 read surfaces (list + detail) feature-complete on Phase 4.0 primitives. |
| 086 | 2026-04-29 | **Phase 4.3 Tier 1 — `/invoices` list revamp.** Route moves from `frontend/src/routes/invoices/+page.svelte` (359-line pre-revamp inline filter bar / table / bulk toolbar / pagination) to [(nexus)/invoices/+page.svelte](frontend/src/routes/(nexus)/invoices/+page.svelte) so it inherits AppShell + Sidebar + TopBar (precedent: iter 076). Four new components in [frontend/src/lib/invoice/](frontend/src/lib/invoice/): `InvoiceListFilters` (Status / Invoice# / PO# / Vendor / DateFrom / DateTo + Clear, vendor filter hidden for VENDOR), `InvoiceListBulkBar` (3-prop simpler than `PoListBulkBar` — only Download PDFs, no Valid Actions matrix), `InvoiceListTable` (responsive: `<table data-testid="invoice-table-desktop">` at desktop, `<div data-testid="invoice-table-mobile" role="list">` of cards at 390px; Phase 4.0 `StatusPill` primitive replaces legacy [frontend/src/lib/components/StatusPill.svelte](frontend/src/lib/components/StatusPill.svelte) which stays as last-consumer-on-`/invoice/:id` until Phase 4.3 Tier 2), `InvoiceListPagination` (mirror of `PoListPagination` shape with 10/20/50 page-size options). `LoadingState` overlay preserves prior rows on filter-refetch (iter 076 G-06 #3 precedent). No header CTA on PageHeader — invoices are created from PO detail, not the list. `isRevampRoute` extended with `pathname === '/invoices'` exact match; the detail route `/invoice/:id` (no trailing s) stays pre-revamp until Tier 2. Existing `frontend/tests/invoice-list.spec.ts` rewritten: 5 selectors-migrated tests + 7 new permanent specs (AppShell mount, empty no-filter / empty with-filter / error+retry / vendor-filter-hidden-for-VENDOR / bulk-clear / pagination-disable-states) + 1 unchanged detail Download-PDF test. Strict-mode duplicate-testid collision between desktop `<tr>` and mobile `<div>` rows resolved cleanly via parent-testid scoping (`page.getByTestId('invoice-table-desktop').getByTestId('invoice-row-...')`) — superior to `PoListTable`'s `tbody tr[data-testid=...]` tag-prefixed pattern that violates the new selector policy. New `invoice-list.spec.ts` uses an explicit `User` import + `mockUser(page: Page, user: User)` param typing so it doesn't inherit the `typeof SM_USER` literal-narrowing pattern that bites the PO test suite. 696 backend (no change) + 282 Playwright (+7). Phase 4.3 Tier 2+ (detail / create / dispute reason flow) and the deferred G-28 / G-29 close-out items remain on backlog. |
| 084 | 2026-04-29 | **`/po/[id]` document attachments (Phase 4.2 G-22).** Four new endpoints under `/api/v1/po/{po_id}/documents` (POST/GET/GET-id/DELETE) wrap iter 035 file storage with PO-aware role + ownership + PO-type checks. Locked vocabulary: PROCUREMENT accepts `SIGNED_PO`, `COUNTERSIGNED_PO`, `AMENDMENT`, `ADDENDUM`; OPEX accepts `SIGNED_AGREEMENT`, `AMENDMENT`, `ADDENDUM`. Locked permission matrix: PROCUREMENT manage by ADMIN/SM/VENDOR(own), view adds PM/FM/QL; OPEX manage by ADMIN/FM only, SM/PM/QL hidden entirely, VENDOR(own) read-only. Cross-vendor VENDOR returns 404 (matches iter 032 invariant) — router calls `check_vendor_access` between PO fetch and the permission helper. New `POAttachmentType` enum + `PROCUREMENT_ATTACHMENT_TYPES` / `OPEX_ATTACHMENT_TYPES` partition + `validate_attachment_type` single source of truth in [backend/src/domain/po_attachment.py](backend/src/domain/po_attachment.py); frontend mirrors at [frontend/src/lib/po/po-attachment-types.ts](frontend/src/lib/po/po-attachment-types.ts). New `PO_DOCUMENT_UPLOADED` activity event with `(LIVE, None)` metadata; router passes per-call `target_role` override (SM for PROCUREMENT, FM for OPEX) per the iter 056 `_counterpart_target` precedent. No `PO_DOCUMENT_DELETED` event (matches iter 046 upload-only precedent). `files.uploaded_by` column added (nullable, idempotent ALTER); PO list endpoint denormalizes `uploaded_by_username` via single batch query (`WHERE id = ANY($1)`). Generic `/api/v1/files` DTOs unchanged; only the new `POFileListItem`/`POFileUploadResponse` DTOs in [backend/src/po_document_dto.py](backend/src/po_document_dto.py) expose the username field. Frontend: `canViewPOAttachments` / `canManagePOAttachments` in [frontend/src/lib/permissions.ts](frontend/src/lib/permissions.ts) — first helpers in that file with `(user, po)` shape (the OPEX-vs-PROCUREMENT branch + VENDOR scope check requires it). `PoDocumentsPanel` self-fetches and renders a hand-rolled `<table>` (DataTable's `render` is still `string | number`; same precedent as `PoListTable` from iter 076 and `PoInvoicesPanel` from iter 083). `PoDocumentUploadDialog` mirrors the iter 081 `PoLineModifyModal` pattern with FormField-wrapped Select (options derived from `po.po_type`) + native PDF file input + client-side 10MB / PDF-only validation. Empty + manage shows `EmptyState` with the upload affordance; empty + view-only hides the panel entirely. Panel mounted on `(nexus)/po/[id]/+page.svelte` between Activity and Rejection History. Panel gained a `mockFiles` bypass prop matching iter 083's `PoActivityPanel.mockEntries` precedent so `/ui-demo/po-documents` renders without backend. Seed extends to attach one `SIGNED_PO` PDF to the first ACCEPTED PROCUREMENT PO and one `SIGNED_AGREEMENT` PDF to one OPEX PO via in-process ReportLab (≤5KB each, no committed binary fixtures). 67 new pytest (629 → 696, +67): domain unit + endpoint permission matrix (28 parametrized rows) + file-type vocabulary mismatch + MIME/size/empty rejection + cross-PO leakage 404 + list ordering DESC + username denormalization + activity row + delete behavior + seed thresholds. 18 new Playwright (239 → 257, +18) covering panel + upload-button visibility per `(role, po_type)` cell, upload happy paths for both PO types, dialog cancel, client-side validation, delete flow, empty states. Resolves G-22. |
| 097 | 2026-04-30 | **Phase 4.6 Tier 1 — `/shipments/[id]` shell port + line items panel.** Last legacy aggregate-detail page ports under `(nexus)`. Route moves from `frontend/src/routes/shipments/[id]/+page.svelte` (313-line legacy with raw `<table>` + inline `<input>` cells + hand-rolled `.status-badge` colors) to [(nexus)/shipments/[id]/+page.svelte](frontend/src/routes/(nexus)/shipments/[id]/+page.svelte) inheriting AppShell + Sidebar + TopBar. Three new components in `frontend/src/lib/shipment/`: `ShipmentDetailHeader` (Phase 4.0 `DetailHeader` + `StatusPill` covering all 5 `ShipmentStatus` values with tone map DRAFT→gray / DOCUMENTS_PENDING→orange / READY_TO_SHIP→blue / BOOKED→blue / SHIPPED→green, `actionRail` snippet carrying both PDF download buttons), `ShipmentMetaPanel` (compact `PanelCard` over `AttributeList` for Marketplace / Created / Updated), `ShipmentLineItemsPanel` (`PanelCard` with edit-mode Save action + transient `StatusPill tone="green" label="Saved"` 3s pill, desktop hand-rolled `<table>` / mobile cards split with mirrored `-mobile` testid suffixes, Phase 4.0 `Input` for text fields + raw `<input type="number">` for `package_count` since `Input` accepts `string` only, drafts re-init via `wasSaving` `$effect` after parent reassigns `lineItems`). New `canEditShipment(role, status)` two-arg helper in `permissions.ts` alongside `canViewPOAttachments`: `is(role, 'SM', 'FREIGHT_MANAGER') && (status === 'DRAFT' || status === 'DOCUMENTS_PENDING')`. Save payload mapping preserved exactly from legacy (trim → null for whitespace-only strings, `package_count` to Number when present). 7 new permanent specs in `frontend/tests/shipment-detail.spec.ts` (page mount, status-pill tone matrix, editable inputs on DRAFT, read-only spans on READY_TO_SHIP, save round-trip, save error inline, PDF download fired). PDF-download specs verified via per-route hit counter polled with `expect.poll()`. `route.fallback()` (not `route.continue()`) needed for per-spec PATCH overrides to chain to the GET handler. Backend untouched. Out of scope: iter 046 frontend (Documents / Readiness / Mark Ready), iter 074 frontend (Booking / Mark Shipped), `/shipments` list page, sidebar slot, deep links from PO detail. iter 098 number is taken by the parallel credential-reset / reissue-invite security iter; A2 (docs / readiness / mark-ready) lands at iter 099 or later. 357 Playwright (+7) + 738 backend (no change). The only remaining pre-revamp routes are `/login` / `/register` / `/setup` (auth-page revamp couples with the iter 096 token migration). |
| 096 | 2026-04-30 | **Invite token replaces username on registration link.** Closes the iter-030 security gap where `/register?username=<name>` let anyone who guessed a PENDING user's username complete registration on their behalf. New nullable `users.invite_token TEXT` column populated by `User.invite()` (UUID v4) and cleared by `User.activate()` so registration is consume-once at the domain layer. `POST /api/v1/auth/bootstrap` and `POST /api/v1/users/invite` now return `invite_token` alongside the user dict; `_user_to_dict` does NOT include it (the token is a secret like a password-reset link, not a profile field). `POST /api/v1/auth/register/options` body shape changed from `{username}` to `{token}` via a new `TokenRequest` pydantic model — sending `{"username": ...}` returns 422 (legacy path closed deliberately). `POST /api/v1/auth/register/verify` body changed from `{credential, username}` to `{credential, token}` and looks up the user via `repo.get_by_invite_token` instead of `get_by_username`. New `UserRepository.get_by_invite_token`. Schema migration is idempotent ALTER + a backfill `UPDATE users SET invite_token = gen_random_uuid()::text WHERE status = 'PENDING' AND invite_token IS NULL` so existing dev DBs with mid-flight invites are not stranded with NULL tokens. Login flow untouched — login does not leak account state to anyone who lacks credentials, and migrating it would have churned every test fixture for no gain. Frontend: `bootstrap`/`registerOptions`/`registerVerify` signatures in [frontend/src/lib/auth.ts](frontend/src/lib/auth.ts) take a token; `/register` page reads `?token=` instead of `?username=`; `/setup` page reuses the token from the bootstrap response in the same round trip (no redirect needed for the very-first admin). Login page untouched. Tests: 11 new permanent specs in `backend/tests/test_invite_token.py` (token generated on invite + bootstrap, token absent from user dict, options + verify keyed on token, unknown token 404, missing token 400, used token cleared and 404 on reuse, second invitee with their own distinct token, username body shape now 422, domain unit `User.activate()` clears + `User.create()` does not allocate); existing `_register_verify` helper in `test_auth_endpoints.py` migrated to take token (3 transitively-broken tests now pass via the helper); `test_register_options_unknown_user_returns_404` renamed to `_unknown_token_returns_404`; one Playwright spec description updated from "without username" to "without token". Out of scope: token expiry (separate stale-PENDING-cleanup iter), ADMIN re-roll endpoint, rate-limiting, multi-passkey, welcome-email-on-invite. New domain term: `InviteToken`. 738 backend (+11) + 350 Playwright (no change). |
| 095 | 2026-04-29 | **User management backend CRUD (parallel to Phase 4.5).** Adds five ADMIN-only endpoints under existing `invite_router` (`/api/v1/users`): `GET /` (with optional `?status=` and `?role=` filters, ordered by username asc), `GET /{user_id}`, `PATCH /{user_id}` (display_name + email; rejects empty/whitespace display_name with 422; `email: null` clears, omitted leaves untouched via `model_fields_set` check), `POST /{user_id}/deactivate`, `POST /{user_id}/reactivate`. Routes appended after `/invite` so route-ordering preserves `POST /api/v1/users/invite`. New `_require_admin(request)` helper matches the manual `request.state.current_user` check pattern already used in `auth.py` (a `require_role` migration is its own future iter). Deactivate handler runs the last-active-admin guard *before* the self-deactivate guard so the only realistic last-admin path (sole admin trying to disable themselves) returns "cannot deactivate the last active admin"; with two admins the self check fires with "cannot deactivate yourself". Domain `ValueError` from `User.deactivate()` / `reactivate()` (already-INACTIVE / not-INACTIVE) maps to 409 with the message. New `UserRepository.list_users(status, role)` builds a single dynamic-WHERE query; existing `list_active_users` kept for dev-login. `_user_to_dict` gained `email` (small accidental gap closed); one existing exact-dict assertion in `test_auth_dev.py::test_dev_login_creates_session_for_active_user` flipped to add the new key (the iter context predicted no existing test breakage; missed it). New `backend/tests/test_user_management.py` with 27 specs covering list/get/patch/deactivate/reactivate happy paths + 403 unauth + 403 non-admin + 404 unknown id + 409 already-inactive + 409 already-active + 409 already-pending + 409 self-deactivate (with two admins) + 409 last-active-admin (with one admin + a PENDING admin to confirm PENDING admins do not count toward the active pool) + invite-route-ordering smoke test. Tests use the dev-login pattern (`DEV_AUTH=1` + `_seed_user` via the dependency-overridden repo) — same path as `test_auth_dev.py`. No domain terms emerged: pure surface expansion over existing `User.deactivate()` / `reactivate()` / `count_active_admins()`. Frontend `/users` page, role/vendor_id/username mutation, multi-passkey, invite-token-as-UUID, vendor-deactivation cascade — all deferred. 727 backend (+27, plus the patched dev-login spec) + 342 Playwright (no change). |
| 082 | 2026-04-29 | **`/po/[id]` post-acceptance + production status revamp (Phase 4.2 Tier 4).** Backend extracts `_OVERDUE_THRESHOLDS` from `dashboard.py` to `domain/milestone.py` as `MILESTONE_OVERDUE_THRESHOLDS`; new `compute_days_overdue` helper next to it. `MilestoneResponse` gains `is_overdue: bool` + `days_overdue: int \| None` computed only for the latest posted milestone (the "stuck" stage). Frontend swaps three pre-revamp blocks on `(nexus)/po/[id]/+page.svelte`: ACCEPTED-PO line items table → `PoLineAcceptedTable` (cards with status pill + cert badge + invoiced/remaining for procurement); inline `<dialog>` → `PoAddLineDialog` (FormField-wrapped Inputs + Country `Select`, server error inline at top, modal pattern reused from iter-081 `PoLineModifyModal`); legacy `MilestoneTimeline` component → `PoMilestoneTimelinePanel` wrapping the Phase 4.0 `Timeline` primitive (which gains a 4th `'overdue'` state with `--dot-red` marker). Gate-closed paths now hide Add Line + per-line Remove (was: disabled with attribute tooltip); SM/ADMIN see a single `po-post-accept-gate-closed-note` panel-level message instead. Vendors never had the affordance and never see the note. HS code and UoM stayed as `Input` (not `Select`) since `ReferenceData` doesn't carry those lists — matches existing `POForm` regex-validated pattern. Legacy `frontend/src/lib/components/MilestoneTimeline.svelte` deleted (189 lines); `PoMilestoneTimelinePanel` derives the next-expected milestone internally and passes it as a typed `ProductionMilestone` enum value via the `onPost` callback. Tier 4 mock at `/ui-demo/po-accepted` ships toggleable role × gate × milestone × po_type matrix. Resolves G-18 + G-19. 10 new pytest (4 `compute_days_overdue` + 3 `MilestoneResponse` shape + 3 list_milestones overdue cases) + 18 new Playwright (1 primitives Timeline overdue + 17 po-detail Tier 4). 629 backend + 223 Playwright green. |

---

## What exists and works

- Full PO lifecycle: create, submit, accept/reject, revise, resubmit
- Full invoice lifecycle: create from PO, submit, approve, pay, dispute, resolve
- Over-invoicing guard (cumulative quantity check)
- OPEX invoicing (free-form lines)
- PDF export for POs and invoices
- Production milestone tracking with strict sequencing
- Activity log powering dashboard feed and notification bell
- Vendor management with types and country validation
- Product catalog (vendor-scoped, certification flag)
- Reference data for all dropdowns
- Bulk actions on PO list
- Dashboard with summary cards, production pipeline, overdue alerts
- Postgres 16 with asyncpg connection pool (migrated from SQLite in 029a)
- Seed script for demo data (`tools/seed_data.py`)
- User entity with 6 roles (ADMIN, SM, VENDOR, FREIGHT_MANAGER, QUALITY_LAB, PROCUREMENT_MANAGER) and 3 statuses (ACTIVE, INACTIVE, PENDING)
- WebAuthn passkey registration/login, cookie sessions (itsdangerous), session middleware
- Bootstrap flow (first user becomes ADMIN), invite-only registration
- API role guards on all endpoints (ADMIN bypass, per-role access control)
- ADMIN user management CRUD on `/api/v1/users`: list (with status + role filters), get, patch (display_name + email), deactivate, reactivate. Last-active-admin guard prevents locking everyone out; self-deactivate guard prevents accidental account loss when not the last admin. Role/vendor_id/username mutation deferred. (iter 095)
- Invite-token registration links: `/register?token=<uuid>` replaces `/register?username=<name>`. Token is a per-invite UUID v4 stored on `users.invite_token`, generated by `User.invite()`/bootstrap, returned only in the invite/bootstrap response, consumed by the registration handshake, and cleared by `User.activate()`. Username is no longer guessable as a registration vector. (iter 096)
- Dev-login endpoint for local development (`GET /api/v1/auth/dev-login`)
- Vendor-scoped data access: VENDOR users see only their vendor's POs, invoices, milestones, activity, and dashboard data (check_vendor_access helper, 404 on ownership mismatch)
- Frontend auth flow: login page (/login), invite-only register page (/register?token=, iter 096), bootstrap page (/setup), layout session check with redirect to /login, logout button, deep link preservation
- All frontend API calls send credentials (cookies) and redirect to /login on 401
- Frontend role-conditional rendering: nav links, page-level redirects, action buttons all respect user role. SM/ADMIN see management controls, VENDOR sees accept/reject/invoice/milestone controls, QUALITY_LAB sees products only, FREIGHT_MANAGER sees POs read-only
- Backend activity endpoints accept optional target_role filter for role-scoped feeds
- Document storage: upload/download/delete/list API backed by local filesystem (`uploads/`), metadata in `files` table with entity_type/entity_id index, PDF-only with 10MB limit, filename sanitization, path traversal protection, require_auth on all endpoints
- PO marketplace field (AMZ, 3PL_1, 3PL_2, 3PL_3) with reference data validation and PO list filter
- LineItem product_id FK to product catalog (loose, no cross-vendor validation yet)
- Vendor address and account_details fields
- Product manufacturing_address field
- QualificationType entity with join table replacing requires_certification boolean; CRUD and product assignment/removal API
- Certificate entity with computed EXPIRED status (not persisted) and document upload; CERT_UPLOADED activity event
- Line-level PO negotiation: vendor and SM exchange modifications over up to 2 rounds via modify_line/accept_line/remove_line/submit_response; force-accept and force-remove overrides available at round 2 for SM; qty=0 on modify routes directly to REMOVED; line_edit_history audit trail per field change
- PackagingSpec with COLLECTED status, file upload, packaging readiness endpoint per product/marketplace; PACKAGING_COLLECTED and PACKAGING_MISSING activity events
- PDF export filters to ACCEPTED lines with MODIFIED stamp on negotiated POs; PO list shows Partial pill when ACCEPTED+REMOVED mix exists and Modified pill during negotiation
- Advance payment gate: payment_terms.has_advance drives whether mark-advance-paid is required; once marked paid or first milestone posted, post-acceptance line add/remove window closes
- Post-acceptance line modification: SM can add or remove lines on ACCEPTED POs while gate is open; removal blocked when any invoice or shipment references the line
- PO line negotiation UI: vendor and SM exchange modifications per line with inline diff, edit-history timeline, force-override at round 2 behind a confirmation dialog; line-level status pills reflect PENDING/MODIFIED_BY_VENDOR/MODIFIED_BY_SM/ACCEPTED/REMOVED
- Email notifications via SMTP: po_accepted, po_modified, po_line_modified, and po_advance_paid templates dispatched from activity events to role-scoped recipients; development mode logs without network access; failures recorded as EMAIL_SEND_FAILED activity rows
- PO submit/resubmit return POSubmitResponse wrapper carrying advisory cert_warnings (per line item, MISSING/EXPIRED). Lines without product_id and POs without marketplace skip the gate. (iter 039)
- QC_PASSED milestone fans out CERT_REQUESTED activity to QUALITY_LAB for products lacking valid certs. (iter 039)
- Shipment aggregate: create from accepted PO with cumulative shipped-quantity guard, multiple shipments per PO allowed, status DRAFT > DOCUMENTS_PENDING > READY_TO_SHIP, per-line weights/dims/package_count/dimensions/country_of_origin editable in DRAFT/DOCUMENTS_PENDING. Frontend shipment detail page (`/shipments/[id]`) on Phase 4.0 primitives since iter 097. (iters 043, 044, 097)
- Packing list PDF generated from shipment + PO + vendor data; ReportLab pattern matches PO/Invoice PDFs. None-valued weights render as "-". (iter 044)
- Export commercial invoice PDF on Shipment with deterministic CI number CI-{shipment_number}; HS code + unit price come from PO line items, weights from shipment line items, line value = qty × unit_price. (iter 045)
- Shipment document requirements + readiness gate: PACKING_LIST and COMMERCIAL_INVOICE requirements auto-seeded on submit-for-documents; SM/FREIGHT_MANAGER can add custom user-defined types. Upload against a requirement transitions it to COLLECTED and records DOCUMENT_UPLOADED activity. mark-ready blocked unless documents + certificates + packaging all pass; 409 returns structured ReadinessResult with missing items. (iter 046, backend only)
- Phase 4.0 primitive library (iters 062-069): design tokens in `global.css`, `(nexus)` SvelteKit layout group with `/_smoke` sentinel route, `/ui-demo` gallery route + `/ui-demo/shell` AppShell preview (none linked from nav). Twenty-six Svelte 5 primitives under `frontend/src/lib/ui/`: 7 leaves, 5 composites (incl. KpiCard with icon slot), 6 display+state, 3 table+headers, 5 shell (Sidebar with sections+footer+roleLabel, TopBar, AppShell, UserMenu) + `sidebar-items.ts` + `redirects.ts`. All use `ui-<name>` scoped outermost class. Tests: `primitives.spec.ts` 33, `sidebar-items.spec.ts` 7, `redirects.spec.ts` 3, `nexus-shell.spec.ts` 3 (46 new primitive-layer tests, 148 Playwright total including pre-revamp flows). Input surfaces `aria-invalid`. LoadingState honors `prefers-reduced-motion`. UserMenu collapses meta on mobile and gates Switch role behind `import.meta.env.DEV`. DataTable generic + server-driven pagination. AppShell composes Sidebar + TopBar + ErrorBoundary + mobile off-canvas drawer. Redirect infrastructure ready for aggregate-phase retirements. No user-facing aggregate page consumes these yet.
- **Phase 4.0 foundation complete (iters 061-070)**: 26 primitives, design tokens, `(nexus)` shell, redirect infrastructure, axe AA clean. Ready for Phase 4.1 (Dashboard) to consume the primitives on a new branch.
- **Phase 4.1 dashboard live (iters 071-073, 075)** at `(nexus)/dashboard`: ADMIN/SM/PROCUREMENT_MANAGER see the four-KPI grid (Pending POs, Awaiting acceptance, In production, Outstanding A/P) with USD value chips on KPIs 1-3, awaiting-acceptance panel, and curated recent activity (line-level negotiation, force transitions, convergence, and email-failures filtered out via `_DASHBOARD_EXCLUDED_EVENTS`). Activity rows render as PO/invoice deep links when the role can view the target, else as plain spans. FREIGHT_MANAGER sees its own four-KPI branch (Ready batches, Shipments in flight, Pending invoices + USD, Docs missing) plus three FM panels. PM activity panel near-empty until events fan out to PROCUREMENT_MANAGER (backlog).
- **Phase 4.2 `/po` list live (iter 076)** at `(nexus)/po`: replaces the pre-revamp inline filter bar / table / pagination / bulk toolbar with `PoListFilters` + `PoListBulkBar` + `PoStatusPills` + `PoListTable` + `PoListPagination` from `frontend/src/lib/po/`. Marketplace filter (4-enum) + responsive layout (table at desktop, stacked cards at 390px) + `LoadingState` overlay preserves prior rows on filter refetch + role-aware selection column and New PO CTA (PROCUREMENT_MANAGER + FREIGHT_MANAGER are read-only). `canBulkPO` permission added. Test selectors migrated to testid surface (`po-filter-*`, `po-bulk-bar`, `po-bulk-action-{action}`, `po-table`, `po-row-{id}`, `po-pagination`, `po-status-partial`, `po-list-loading`, `po-page-header-action`).
- **Phase 4.2 PO detail Tier 2 live (iter 077)**: at `(nexus)/po/[id]`, header + role-aware action rail (inline at desktop ≥1024px, sticky bottom at <1024px) + advance payment panel gated by Post-Acceptance Gate (backend `can_modify_post_acceptance`) + cert warnings banner from `POSubmitResponse.cert_warnings`. Legacy sections (milestone timeline, invoices, rejection, activity) remain pre-revamp shape.
- **Phase 4.2 PO line negotiation Tier 3 live (iter 081)**: at `(nexus)/po/[id]`, line items render as cards with status pills (PENDING/MODIFIED_BY_VENDOR/MODIFIED_BY_SM/ACCEPTED/REMOVED), per-line action row (Modify/Accept/Remove + Force Accept/Force Remove for SM at round 2), single "View changes" toggle revealing both diff and round-by-round history, sticky `PoSubmitResponseBar` with round indicator + addressed-lines gating + accepted/modified/removed delta confirm. Modify modal validates non-empty UoM/Description/HS-code/Country and routes qty=0 to a "Remove line" submit. Mobile (390px) stacks actions two-per-row, bar pinned above iOS safe area. Replaces all five iter-057 negotiation components.
- **Phase 4.2 PO detail Tier 5 (finishing pass) live (iter 083)**: at `(nexus)/po/[id]`, the last four pre-revamp blocks ship as Phase 4.0 primitive consumers. `PoMetadataPanels` renders Trade Summary + Buyer + Vendor + Trade Details + Terms in five `PanelCard`s (the first four use `AttributeList`). `PoRejectionHistoryPanel` wraps a `PanelCard` over latest-first `<article>` rows. `PoInvoicesPanel` wraps a `PanelCard` over a hand-rolled `<table>` (DataTable's `render` is `string \| number` only — snippet support stays on the backlog) with subtitle "{n} units remaining to invoice" for PROCUREMENT and "OPEX — single invoice" for empty OPEX. `PoActivityPanel` wraps a `PanelCard` over the Phase 4.0 `ActivityFeed` primitive, fetches activity on mount, shows 10 entries with a Show more Button (variant ghost) up to the full set. Shared `frontend/src/lib/po/event-labels.ts` carries `EVENT_LABELS` + `categoryToTone` so the panel + future PO event surfaces share one dictionary.
- **Phase 4.3 Tier 1 `/invoices` list live (iter 086)** at `(nexus)/invoices`: replaces the pre-revamp inline filter bar / table / bulk toolbar / pagination with `InvoiceListFilters` + `InvoiceListBulkBar` + `InvoiceListTable` + `InvoiceListPagination` from `frontend/src/lib/invoice/`. Bulk PDF download + filter narrowing + responsive layout (table at desktop, stacked cards at 390px) + role-conditional vendor filter (hidden for VENDOR users since data is already vendor-scoped) + dashboard-grade `LoadingState`/`EmptyState`/`ErrorState`. Test selectors on the testid namespace (`invoice-filter-*`, `invoice-table-desktop`, `invoice-row-{id}`, `invoice-row-checkbox-{id}`, `invoice-bulk-bar`, `invoice-bulk-action-download`, `invoice-pagination-*`).
- **Phase 4.6 Tier 1 `/shipments/[id]` shell port live (iter 097)**: last legacy aggregate-detail page mounts under `(nexus)` AppShell. Three new components in `frontend/src/lib/shipment/`: `ShipmentDetailHeader` (Phase 4.0 `DetailHeader` + 5-state `StatusPill` tone map: DRAFT→gray / DOCUMENTS_PENDING→orange / READY_TO_SHIP→blue / BOOKED→blue / SHIPPED→green; actionRail snippet hosts both PDF buttons), `ShipmentMetaPanel` (`AttributeList` over Marketplace/Created/Updated), `ShipmentLineItemsPanel` (desktop `<table>` / mobile cards split, edit-mode Save action with transient 3s "Saved" pill, drafts re-init via `wasSaving` `$effect` after parent reassigns `lineItems`, raw `<input type="number">` for `package_count` since `Input` accepts `string` only). New `canEditShipment(role, status)` two-arg helper. No new functional surface — iter 046 frontend (Documents / Readiness / Mark Ready) and iter 074 frontend (Booking / Mark Shipped) follow as separate iters. The only remaining pre-revamp routes are `/login` / `/register` / `/setup`.
- **Phase 4.5 Tier 4 `/products/[id]/edit` certificates panel live (iter 094)**: cert UI fold-in from iter 040 finally lands. New `ProductCertificatesPanel` (PanelCard + EmptyState + qualification-grouped rows + StatusPill PENDING→gray / VALID→green / EXPIRED→red + SM-only Upload PDF gated on `document_id === null` + inline Add form on Phase 4.0 FormField + Input + Select + DateInput + Button + `wasAdding` `$effect` auto-close-on-success). Backend `CertificateListItem` DTO extended with `document_id` so the panel can decide upload visibility from a single list call. New API client `listCertificates` / `createCertificate` / `uploadCertificateDocument` (multipart, 10MB + PDF MIME client-side validation). Cert edit / PENDING→VALID approve / dashboard expiry alerts deferred to follow-up iters. With this iter the entire `/products/*` namespace is feature-complete on Phase 4.0.
- **Phase 4.5 Tier 3b `/products/[id]/edit` packaging specs panel live (iter 093)**: retires the last legacy block on the edit page. New `ProductPackagingSpecsPanel` (PanelCard + EmptyState + marketplace-grouped rows + StatusPill PENDING→gray / COLLECTED→green + SM-only Delete gated on PENDING + inline Add form on Phase 4.0 FormField + Input + native textarea + Button + `wasAdding` `$effect` auto-close-on-success). Native `window.confirm` for delete (no Phase 4.0 confirm dialog primitive yet). The only remaining pre-revamp routes are `/shipments/*` and `/login` / `/register` / `/setup`.
- **Phase 4.5 Tier 3a `/products/[id]/edit` live (iter 092)**: edit page ports under `(nexus)` AppShell with two new components: `ProductEditForm` (PanelCard with readonly vendor + part_number, editable description + manufacturing_address, sticky-on-mobile footer) and `ProductQualificationsPanel` (PanelCard + StatusPill target_market badges + SM-only Add/Remove). Packaging specs block stays inline as legacy markup until iter 093 retires it. After this iter the entire `/products/*` namespace renders under (nexus); the only remaining pre-revamp routes are `/shipments/*` and `/login` / `/register` / `/setup`.
- **Phase 4.5 Tier 2 `/products/new` live (iter 091)**: `/products/new` ports under `(nexus)` AppShell. New [`frontend/src/lib/product/ProductCreateForm.svelte`](frontend/src/lib/product/ProductCreateForm.svelte) mirrors iter 089 `VendorForm` (PanelCard + FormField + Input + Select + native textarea + sticky-on-mobile footer). Vendor select shows ACTIVE vendors only (legacy `listVendors({ status: 'ACTIVE' })` constraint preserved). Inline 409 conflict copy preserved verbatim. Edit form `/products/[id]/edit` (436 lines, biggest page) stays pre-revamp until iter 092.
- **Phase 4.5 Tier 1 `/products` list live (iter 090)**: `/products` ports under `(nexus)` AppShell. Two new components in [frontend/src/lib/product/](frontend/src/lib/product/): `ProductListFilters` (vendor select + Clear), `ProductListTable` (responsive desktop-table / mobile-cards, qualification badge as Phase 4.0 `StatusPill` reading `qualifications.length`). Fixes a long-standing dead-field bug — the legacy page read `product.requires_certification` which has not existed on `ProductListItem` since iter 036a. `/products/new` (iter 091) and `/products/[id]/edit` (iter 092) stay pre-revamp until their iters land.
- **Phase 4.4 Tier 1 `/vendors` + `/vendors/new` live (iter 089)**: both routes mount under `(nexus)` AppShell + Sidebar + TopBar. Three new components in [frontend/src/lib/vendor/](frontend/src/lib/vendor/): `VendorListFilters` (status + type + clear), `VendorListTable` (responsive table-at-desktop / cards-at-mobile, Phase 4.0 `StatusPill` and `Button`s, shared `vendor-row-{id}` testids), `VendorForm` (Phase 4.0 form primitives + native `<textarea>` for address/account_details + sticky-on-mobile footer). Legacy `frontend/src/routes/vendors/` tree retired. Vendor list filters narrow client-side via `$effect`, error+retry path uses `ErrorState`, empty states differentiate "no vendors yet" from "no matching vendors". `vendor.spec.ts` migrated 9 → 17 specs on the new testid namespace.
- **Phase 4.3 Tier 3 invoice create modal live (iter 088)**: clicking "Create Invoice" from `PoActionRail` on a VENDOR / ACCEPTED / PROCUREMENT PO opens [`InvoiceCreateModal`](frontend/src/lib/invoice/InvoiceCreateModal.svelte) — Phase 4.0 `Button` + `Input` primitives, desktop `<table>` / mobile `<ul>` cards split, qty pre-filled to remaining per line, lines with `remaining === 0` rendered disabled, submit clamps at `Math.min(parsed, remaining)`, non-zero rows POSTed and route navigates to `/invoice/{id}`. OPEX flow unchanged (skips the modal). Legacy `frontend/src/lib/components/CreateInvoiceDialog.svelte` deleted. Phase 4.3 invoice surface (list + detail + create) feature-complete.
- **Phase 4.3 Tier 2 `/invoice/:id` detail live (iter 087)** at `(nexus)/invoice/[id]`: replaces the pre-revamp section-card layout with seven components from `frontend/src/lib/invoice/`. `InvoiceDetailHeader` (back link to `/invoices`, invoice number title, vendor·PO·created subtitle, Phase 4.0 StatusPill). `InvoiceActionRail` role × status matrix: VENDOR sees Submit on DRAFT, SM sees Approve + Dispute on SUBMITTED / Pay on APPROVED / Resolve on DISPUTED, all roles see Download PDF (inline at desktop ≥768px, sticky-bottom at <768px). `InvoiceDisputeModal` replaces legacy `DisputeDialog`. `InvoiceMetadataPanel`, `InvoiceDisputeReasonPanel` (visible only on DISPUTED), `InvoiceLineItemsPanel`, `InvoiceActivityPanel` (mirror of `PoActivityPanel` with Show more pagination). Shared event-label dictionary promoted to `$lib/event-labels.ts`. Legacy `StatusPill`, `DisputeDialog`, `ActivityTimeline` deleted; `PoInvoicesPanel` migrated to Phase 4.0 StatusPill. Phase 4.3 read surfaces (list + detail) feature-complete on Phase 4.0 primitives.
- **PO create + edit retrofit (iter 085)** at `(nexus)/po/new` and `(nexus)/po/[id]/edit`: both routes mount under AppShell + Sidebar + TopBar via the `(nexus)` group. New [frontend/src/lib/po/PoForm.svelte](frontend/src/lib/po/PoForm.svelte) replaces the legacy 532-line `frontend/src/lib/components/POForm.svelte` and uses Phase 4.0 `PanelCard` + `FormField` + `Input` + `Select` + `DateInput` + `Button` primitives across four sections (Details / Trade / Terms / Line Items) plus a sticky-on-mobile footer. `mode: 'create' \| 'edit-draft' \| 'edit-revise'` discriminates field disability — PO Type `Select` disabled on edit, Marketplace hidden on OPEX, Vendor disabled until PO Type chosen with inline cleared-vendor hint. Payment Terms options append " — advance required" suffix when `has_advance` is true. Dirty-tracking via JSON snapshot diff opens a discard modal on Cancel; pristine Cancel navigates without prompt. Cancel destination branches `/po` for create vs `/po/[id]` for edit. Edit page now accepts DRAFT in addition to REJECTED per ddd-vocab; DRAFT submits as "Save Draft" and REJECTED submits as "Save & Revise" with `PoRejectionHistoryPanel` above the form. Non-editable statuses render a "Back to detail" panel. Phase 4.2 PO surface (list + detail + create + edit) now feature-complete on Phase 4.0 primitives.
- **PO document attachments (iter 084)** at `(nexus)/po/[id]`: Documents `PanelCard` between Activity and Rejection History, backed by four endpoints under `/api/v1/po/{po_id}/documents` wrapping iter 035 file storage with PO-aware role + ownership + PO-type checks. `POAttachmentType` partition: PROCUREMENT accepts `SIGNED_PO`/`COUNTERSIGNED_PO`/`AMENDMENT`/`ADDENDUM`; OPEX accepts `SIGNED_AGREEMENT`/`AMENDMENT`/`ADDENDUM`. Permission matrix: PROCUREMENT manage by ADMIN/SM/VENDOR(own), view adds PM/FM/QL; OPEX manage by ADMIN/FM only, SM/PM/QL hidden, VENDOR(own) read-only. Cross-vendor VENDOR returns 404 (router calls `check_vendor_access` between PO fetch and permission helper). `files.uploaded_by` column populated; list endpoint denormalizes `uploaded_by_username` via single batch query. `PO_DOCUMENT_UPLOADED` activity event (LIVE) with per-call `target_role` override (SM for PROCUREMENT, FM for OPEX). Generic `/api/v1/files` endpoints unchanged and still any-authed. Seed attaches one SIGNED_PO PDF to a PROCUREMENT PO and one SIGNED_AGREEMENT PDF to an OPEX PO via in-process ReportLab. Frontend `canViewPOAttachments` / `canManagePOAttachments` introduce a `(user, po)` helper signature in `permissions.ts` (the file's prior helpers were all `(role)`-shaped).
- **Phase 4.2 PO post-acceptance + production status Tier 4 live (iter 082)**: at `(nexus)/po/[id]`, ACCEPTED-PO line items render via `PoLineAcceptedTable` (cards matching Tier 3 rhythm with cert badge, status pill, invoiced/remaining for procurement). Add Line opens `PoAddLineDialog` with FormField-wrapped Inputs + Country `Select`. Add Line and per-line Remove are HIDDEN (not disabled-with-tooltip) when the post-acceptance gate is closed; SM/ADMIN see a single panel-level `po-post-accept-gate-closed-note` instead. Production Status renders via `PoMilestoneTimelinePanel` wrapping the Phase 4.0 `Timeline` primitive; `Timeline` gains a 4th `'overdue'` state. Backend `MILESTONE_OVERDUE_THRESHOLDS` single-sourced in `domain/milestone.py`; `MilestoneResponse` carries `is_overdue` + `days_overdue` computed only for the latest posted milestone. Legacy `frontend/src/lib/components/MilestoneTimeline.svelte` deleted (189 lines).
- **Dev quick-login (iter 079)**: `POST /api/v1/auth/dev-login` + `GET /api/v1/auth/dev-users` gated by `DEV_AUTH=1` env var; `/login` renders a quick-login row when the endpoint returns a list, hidden when 404. Production-invisible. Replaces the earlier `GET /dev-login` from iter 031 which returned JSON without redirect.
- **Primitive accessible-name convention (iter 080)**: 15 primitives in `frontend/src/lib/ui/` now expose accessible names via three patterns (controls → `ariaLabel` prop; list/table containers → `label` prop; titled containers → uuid + `aria-labelledby` on visible heading). `DetailHeader` and `PageHeader` gain explicit `role="banner"`. CLAUDE.md "Primitive accessible-name convention" section codifies the patterns so new primitives follow them from day one.

## What does not exist yet

### Phase 4 UI revamp (iters 049-055, in planning by user)
The full design system + page redesign covering every existing page (~22 routes). User is drafting the plan based on a Lovable mock. The following backend-complete features are waiting on Phase 4 to ship their UI:
- **Iter 040 — Certificate UI**: ~~product detail qualification list~~ (iter 092), ~~certificate upload flow~~ (iter 094), ~~PO creation cert-warning banner~~ (iter 077). **Still missing:** dashboard expiry alerts (cert expiry KPI + activity feed surfacing CERT_UPLOADED / CERT_REQUESTED), cert metadata edit + PENDING→VALID approve workflow.
- **Iter 046 frontend**: Documents section per shipment with status pills + Generate / Upload actions, Add Requirement button, Readiness panel (Documents / Certificates / Packaging pass-fail with details), Mark Ready to Ship button gated by readiness. Iter 097 ported `/shipments/[id]` to `(nexus)` shell + line items panel as Phase 4.6 Tier 1 (A1); A2 (this iter 046 frontend) lands at iter 099 or later since iter 098 number is taken by the credential-reset security iter.
- **Iter 048 dashboard**: shipment counts by status (DRAFT / DOCUMENTS_PENDING / READY_TO_SHIP), certificate expiry alerts, packaging collection progress per marketplace, notification bell routing for new event types (CERT_REQUESTED, DOCUMENT_UPLOADED, etc.).

### From the backlog (PO confirmation module)
- Overdue PO status (time-based trigger past required delivery date)
- Mobile layout
- Custom value approval for reference data dropdowns
- Dedicated `/api/v1/po/ids` endpoint for cross-page selection beyond 200
- Live/historical exchange rates
- Buyer as first-class entity (currently hardcoded)

### From the backlog (auth and user management)
- Multiple passkeys per user (register backup device for recovery)
- Admin re-invite flow for lost device (reset to PENDING, user registers new passkey)
- User management frontend page (`/users`) — list, invite, deactivate, reset credentials. Backend CRUD shipped iter 095; nav slot reserved.
- VENDOR user's vendor gets deactivated — make vendor-scoped data read-only
- Stale PENDING user cleanup (invited but never registered)
- Proxy access for internal leave coverage (delegation table, time-bounded, audit trail)
- Email/notification for invite links (manual link sharing works for small teams)
- PROCUREMENT_MANAGER role permissions: enum value exists from iter 030, no endpoint guards wired. Future iteration to define and wire access (read-only PO/vendor/invoice visibility, pay/dispute invoices). Frontend: define redirect behavior for PM on non-dashboard pages.
- User management page (`/users`): ADMIN can list, invite, deactivate, reactivate users, reset credentials. Nav "Users" link hidden until page exists.
- ADMIN re-roll invite token: regenerate `users.invite_token` on demand so ADMIN can re-share the link if the original is lost. Pairs with `POST /users/{id}/reset-credentials` (next bullet); both are likely the same iter.
- Credential reset endpoint: `POST /api/v1/users/{id}/reset-credentials` (revoke all passkeys, set status back to PENDING, allocate fresh `invite_token`). Recovery path for lost devices.
- Welcome email on invite: send registration link via email when admin invites a user. Currently manual link sharing.
- ADMIN inherits all actions: `canPostMilestone` is currently exact-match VENDOR only. ADMIN should be able to exercise any role-scoped action (post milestones, submit vendor responses, etc.) for support and debugging. Audit every `isExact` usage in `permissions.ts` and the backend role guards; convert to `is(role, ...) || role === 'ADMIN'` unless there is a concrete reason to keep a capability VENDOR-only.

### From the backlog (UX)
- Error handling across all users and workflows: define and surface user-facing error states for every endpoint (validation errors, conflict errors, not-found, auth failures). Currently errors are ad hoc per endpoint with no consistent frontend treatment.
- Recent activity redesign: club PO and invoice activity into a unified feed (currently separate activity streams). Needs discussion on grouping, filtering, and presentation.
- Deep link preservation on register/bootstrap: deep link preservation on /login is done (iter 033). If an invited user opens a deep link (e.g. `/po/123`) and lands on `/register` or `/setup`, the original path is still lost. After registration/bootstrap they go to `/dashboard`, not the deep link. Low priority since registration is a one-time event.
- Session cookie path/domain audit: verify the session cookie is set with `path=/` and appropriate domain/SameSite attributes for production deployment behind a reverse proxy. Currently works in dev via Vite proxy.
- DataTable primitive extension: promote selection column + snippet cell support out of `PoListTable` and `PoInvoicesPanel` into the shared `frontend/src/lib/ui/DataTable.svelte` primitive, so other list pages can adopt the same selection contract without copy-paste. Phase 4.x cleanup task.
- PO document versioning: re-uploading a SIGNED_PO does not supersede the prior row — both rows persist, ordered by `uploaded_at DESC`. Cleanup is manual delete. Revisit when users complain about clutter or audit-trail confusion.
- PO_DOCUMENT_DELETED activity event: iter 084 records uploads only, matching the iter 046 shipment-document-upload precedent. Add a delete event when anyone needs deletion history.
- Email template for PO_DOCUMENT_UPLOADED: iter 084 records the activity row but does not invoke `NotificationDispatcher`. Wire SMTP fan-out (likely to SM for PROCUREMENT, FM for OPEX, mirroring the per-call target_role) when the workflow needs out-of-app notification.
- Shared mock-fetch helper for `/ui-demo/*` gallery: iter 084's `/ui-demo/po-documents` patches `globalThis.fetch` for upload/delete demo flows and restores on cleanup. Centralizing this would let future demo routes drop the boilerplate.
- ~~Legacy `frontend/src/lib/components/ActivityTimeline.svelte` and `frontend/src/lib/components/StatusPill.svelte` retire when `invoice/[id]` revamps.~~ Resolved iter 087: all three legacy components (`ActivityTimeline`, `StatusPill`, `DisputeDialog`) deleted; `PoInvoicesPanel` migrated to Phase 4.0 `StatusPill`.
- Backend per-entity activity pagination: `/api/v1/activity/?entity_type=PO&entity_id={id}` returns the full list. Iter 083's `PoActivityPanel` does client-side "Show more" over a single fetch. Revisit when a single PO accumulates >100 events.
- FREIGHT_MANAGER PO detail visibility: today `canViewPOs` includes FREIGHT_MANAGER for the iter 073 ready-batch click-through (FM lands on a PO detail to plan shipping). FM sees an action rail with Download PDF only. Revisit whether FM should see PO detail at all or whether the ready-batch surface should expose only the shipping-relevant fields directly without exposing the full PO. Tier 0 G-28 (role coverage matrix) territory.

### From the backlog (infrastructure)
- File endpoint role guards: restrict upload/download/delete by role and entity ownership on the generic `/api/v1/files/...` surface. Currently any authenticated user can access any file there. Iter 084 tightened the PO surface only via PO-scoped endpoints at `/api/v1/po/{po_id}/documents/...`; the certificate (iter 038) / packaging (iter 042) / shipment (iter 046) consumers still hit the open generic endpoint. Either define per-entity-type guards inside the generic router or migrate each consumer to its own scoped router (the iter 084 pattern).
- File upload entity existence validation: upload endpoint accepts any entity_type/entity_id without checking the entity exists. Decide per-feature whether to validate at upload time or at the consuming feature level.
- Vendor catalog / vendor-SKU mapping: products are vendor-agnostic, but a vendor's catalog defines which SKUs they offer. Line item product_id should reference a SKU in the vendor's catalog. New entity needed.
- HTTPS for non-localhost deployment (WebAuthn requires HTTPS or localhost; needed before first external demo)
- Database migration tool (alembic or similar; needed once real data exists that can't be dropped and recreated)
- Session revocation ("log out all devices"; currently relies on cookie expiry + user status check)
- Self-service vendor onboarding (currently invite-only; needed if vendors should sign up without admin)
- Remove dev-login endpoint before production deployment

### From the roadmap (post-confirmation)
1. **Quality labs frontend** — backend done (iters 036a, 038, 039); frontend cert UI is iter 040 (folded into Phase 4).
2. **Bill of Lading + Certificate of Origin + Insurance Certificate + EEI/AES** — additional shipment document types beyond packing list and commercial invoice. Iter 046 supports user-defined document_type strings, so adding these is configuration; system-generated BoL or chamber-of-commerce CoO integration would be new iters.
3. **Shipment document validation (agents/OCR)** — AI reads uploaded docs, flags discrepancies against shipment + PO data.
4. **Invoice upload** — accept vendor-uploaded invoice files (PDF/image).
5. **Invoice validation (agents)** — AI cross-checks uploaded invoice against PO/shipment data.
6. **Consolidation algorithm** — combine multiple shipments (explicitly deferrable).
7. **Packaging file collections for AMZ shipments** — Amazon-specific packaging requirement templates beyond the generic PackagingSpec.
8. **Shipment booking** — carrier selection, booking creation.
9. **Shipment tracking** — carrier integration, status updates, ETA.
10. **Customs validation** — HS code verification, origin/destination rules, compliance checks.

### Dependencies in the roadmap
- Shipment doc validation (3) needs an uploaded-doc corpus from iter 046 in production use first.
- Invoice upload (4) before invoice validation (5).
- Shipment booking (8) before tracking (9).
- HS codes + country of origin (already on PO line items + shipment line items) feed customs validation (10).

### The agent layer (items 3, 5, 10)
These are the product differentiators. Items 3, 5, and 10 read uploaded documents, cross-reference them against structured data (PO, shipment, certificates), and flag discrepancies. The CRUD workflow is the infrastructure that produces the structured data those agents read.

---

## Domain terms introduced by iteration

| Iteration | New domain terms |
|-----------|-----------------|
| 01-02 | PurchaseOrder, LineItem, POStatus (Draft/Pending/Accepted/Rejected/Revised) |
| 03-04 | Vendor, VendorStatus, ReferenceData (currencies, incoterms, payment terms) |
| 05 | PODocumentExport |
| 06 | PaginatedList, POSearch |
| 07 | Dashboard (read model) |
| 08-10 | BulkAction, CrossPageSelection, ValidActions |
| 11 | VendorType (Procurement/OpEx/Freight/Misc), POType |
| 12 | Invoice, InvoiceLineItem, InvoiceStatus, OverInvoicingGuard, DisputeReason |
| 13 | ReferenceLabel |
| 14 | RemainingQuantity |
| 16 | InvoiceDocumentExport |
| 17-19 | Milestone (5 stages), MilestoneUpdate, MilestoneOrderEnforcement, OverdueProduction |
| 20 | HSCodeFormat |
| 21 | OPEXInvoice |
| 23-24 | ActivityLogEntry, EventType, NotificationCategory, TargetRole |
| 25 | VendorCountryValidation |
| 27 | FieldImmutability (mutable-by-status rules) |
| 29 | Product, RequiresCertification |
| 030 | User, UserRole (6 values), UserStatus (3 values), WebAuthnCredential, SessionCookie, Bootstrap |
| 031 | RoleGuard, RequireRole, RequireAuth |
| 032 | VendorScopedAccess |
| 034 | RolePermission, RoleConditionalRendering |
| 035 | FileMetadata, EntityAttachment |
| 036 | Marketplace, ManufacturingAddress, VendorAccountDetails |
| 036a | QualificationType, ProductQualification (join) |
| 037 | LineItemStatus (Pending/Accepted/Rejected), AcceptLinesRequest |
| 056 | LineItemStatus (new: MODIFIED_BY_VENDOR/MODIFIED_BY_SM/REMOVED), POStatus.MODIFIED, LineEditHistory, LineEditHistoryEntry, NegotiationRound, RoundCount, LastActorRole, ForceOverride, Convergence, EDITABLE_LINE_FIELDS, HandOff |
| 057 | LineNegotiationRow, ModifyLineModal, LineDiff, EditHistoryTimeline, SubmitResponseBar (UI components); CanActOnNegotiation; ForceOverrideConfirmation |
| 041 | PackagingSpec, PackagingSpecStatus |
| 038 | Certificate, CertificateStatus (Pending/Valid/Expired-computed) |
| 042 | PackagingCollection, PackagingReadiness |
| 058 | ActivityEvent.{PO_LINE_MODIFIED, PO_LINE_ACCEPTED, PO_LINE_REMOVED, PO_FORCE_ACCEPTED, PO_FORCE_REMOVED, PO_MODIFIED, PO_CONVERGED}, PartialPill, ModifiedPill, PDFAcceptedOnly |
| 059 | PaymentTermMetadata, AdvancePayment, RequiresAdvance, PostAcceptanceGate, DownstreamArtifact, LineHasDownstreamArtifactError, FirstMilestonePostedAt, PO_ADVANCE_PAID, PO_LINE_ADDED_POST_ACCEPT, PO_LINE_REMOVED_POST_ACCEPT |
| 060 | EmailService, NotificationDispatcher, EmailTemplate, RecipientResolution, FakeEmailService, EMAIL_SEND_FAILED, SMTP_ENV_VARS |
| 043 | Shipment, ShipmentLineItem, ShipmentStatus (DRAFT/DOCUMENTS_PENDING/READY_TO_SHIP), ShipmentNumber, CumulativeShippedQuantityGuard |
| 039 | CertWarning, CertWarningReason (MISSING/EXPIRED), POSubmitResponse, QualityGate, CERT_REQUESTED, TargetRole.QUALITY_LAB, TargetRole.FREIGHT_MANAGER |
| 044 | ShipmentLineItem.{net_weight, gross_weight, package_count, dimensions, country_of_origin}, PackingListPDF, ShipmentLineItemUpdate |
| 045 | CommercialInvoicePDF, CINumber (deterministic CI-{shipment_number}, not persisted) |
| 046 | ShipmentDocumentRequirement, DocumentRequirementStatus (PENDING/COLLECTED), ReadinessResult, AutoGeneratedRequirement, DOCUMENT_UPLOADED, EntityType.SHIPMENT, MarkReadyGate |
| 074 | ShipmentStatus.BOOKED, ShipmentStatus.SHIPPED, BookingMetadata (carrier, booking_reference, pickup_date), ShippedAt, BookShipment, MarkShipped, SHIPMENT_BOOKED, SHIPMENT_SHIPPED, ProductionMilestone.READY_FOR_SHIPMENT (renamed from READY_TO_SHIP) |
| 079 | DevAuthFlag (env-gated, DEV_AUTH=1) |
| 080 | AccessibleName, AriaLabelledBy |
| 082 | MilestoneOverdueThreshold, IsOverdue, DaysOverdue |
| 084 | POAttachment, POAttachmentType, ProcurementAttachmentTypes, OpexAttachmentTypes, AttachmentVocabulary, UploaderUsernameDenormalization, PO_DOCUMENT_UPLOADED |
| 086 | (none — structural retrofit; UI-layer concepts only) |
| 087 | (none — structural retrofit; UI-layer concepts only) |
| 088 | (none — structural retrofit; UI-layer concepts only) |
| 089 | (none — structural retrofit; UI-layer concepts only) |
| 090 | (none — structural retrofit; UI-layer concepts only) |
| 091 | (none — structural retrofit; UI-layer concepts only) |
| 092 | (none — structural retrofit; UI-layer concepts only) |
| 094 | CertificateListItem.document_id (DTO surface; existing domain field exposed on the list response) |
| 096 | InviteToken (UUID v4, per-PENDING-user secret on `users.invite_token`, generated by `User.invite()`, cleared by `User.activate()`, consume-once, never returned in `_user_to_dict`) |
| 097 | (none — structural retrofit; UI-layer concepts only) |
| 085 | (none — structural retrofit; UI-layer concepts only) |
